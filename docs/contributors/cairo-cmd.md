# cairo-cmd

## Current

- **Role:** FE
- **Branch:** `cairo-cmd-fix-87-discover-loading`
- **Task:** Fix #87 — Discover tab flashes empty state "Contribute to repos..." / "Star repos..." on cold cache before fresh data arrives (extension FE-only)
- **Blockers:** None
- **Last updated:** 2026-04-20

## Decisions

- 2026-04-13: Chose Option A (centered minimal layout) for welcome screen — clean list layout fits sidebar best, avoids scroll, native VS Code feel
- 2026-04-13: Removed stats, founder quote, and permissions disclosure — spec scoped to logo + tagline + 3 value props + CTA only
- 2026-04-13: Using Codicons (comment-discussion, star, organization) instead of emoji — follows design rules
- 2026-04-13: Logo using star SVG placeholder — final GitChat logo asset to be swapped in when provided
- 2026-04-14: Replaced SVG star placeholder with actual GitChat logo (media/icon.png) — added border + increased size to 56px
- 2026-04-14: Enhanced onboarding UI — animated RGB glow border, staggered entrance choreography, breathing icon glows, shimmer effects, smooth dismiss animation
- 2026-04-17: Developed feature to show full seen-by list (all users who have read the message, instead of just avatar stack) + fixed related seen receipts issues
- 2026-04-20: Continued fixing issues arising from the full seen-by list feature
- 2026-04-15: Fix #31/#32 Seen avatars wrong users (`gitchat-webapp/backend` + `gitchat_extension`) — backend emits `conversation:read`/`reaction:updated` into room `conversation:<id>` but payload is missing `conversationId`; extension subscribes to multiple conv rooms simultaneously via `_subscribedConversations` Set → events leak between conversations. Client relay `explore.ts:1695` only checks `if (_activeChatConvId)` without checking `data.conversationId === _activeChatConvId`. Fix 2-repo: (a) **BE** `messages.service.ts` adds `conversationId` to 3 emit payloads (CONVERSATION_READ line 1245, REACTION_UPDATED add 1745 + remove 1797) — additive since `data: Record<string, any>`, backwards compatible with webapp frontend. (b) **Extension** 7 edits: `realtime/index.ts` types + parsers require `conversationId` or drop event; `explore.ts` 2 filter conditions; `sidebar-chat.js` 3 hardening — (i) line 3661 removes `|| _state.otherReadAt` fallback leak between convs (root cause #2 of #32 "DM offline recipient shows seen" — client-only bug BE doesn't cover), (ii) line 3667 filter `readReceipts` init by `groupMembers`/`otherLogin`, (iii) line 3835 drop fallback `|| _state.otherLogin` and validate `readLogin` belongs to the conversation. Deploy order required: BE first — shipping extension first means `data.conversationId === _activeChatConvId` is always false → seen avatar fully disabled. Out of scope (follow-up issue): `sidebar-chat.js:3849-3862` marks all `.gs-sc-msg-out` as seen without comparing timestamps → messages sent after reading get incorrectly marked.
- 2026-04-16: Fix #87 Discover tab empty-state flash (`gitchat_extension` FE-only) — root cause: `src/webviews/explore.ts:352-366, :388-402` already sent `stale: boolean` on each `setStarredReposData` / `setContributedReposData` payload but the `media/webview/explore.js` handler dropped that field, render logic only had 3 branches (error / length===0 / has-items) → could not distinguish "currently fetching" from "genuinely 0 repos" → cold cache always flashed "Contribute to repos to join their teams" (and "Star repos..." for Communities) before fresh data arrived. Fix: 5 edits in 1 file `explore.js`: (1) add `starredReposLoading = true`, `contributedReposLoading = true` state vars (init true); (2) reset 2 flags to true before each `fetchStarredRepos`/`fetchContributedRepos` dispatch when user enters Discover tab (fixes re-entry case); (3) handler derives `loading = stale && !error` — stale=true keeps loading, fresh or error turns loading off; (4) Teams render adds loading branch (error > loading > empty > all-joined); (5) Communities render same pattern. Reused `.gs-empty gs-text-sm` + `codicon-loading codicon-modifier-spin` (already present in People search line 1050) — 0 new components, 0 new CSS. Rejected issue's Option B (BE waits for fresh before returning) as it would lose the stale-while-revalidate benefit for warm-cache users. Rejected adding BE field `isLoading` — not needed since `stale` is already a sufficient signal. Edge case: if stale payload has items, still renders items immediately rather than hiding behind spinner (better perceived perf — loading only shows when `loading && length===0`). Verification manual in Extension Development Host (no webview JS test harness in repo).
