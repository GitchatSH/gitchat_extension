# cairo-cmd

## Current

- **Role:** FE
- **Branch:** `plan/channel_feature`
- **Task:** Re-apply Fix #106 (ChatPanel orphan cleanup) — prior commit `de6d187` lost (not on any branch, only in reflog); re-done on current branch. Awaiting user review before commit.
- **Blockers:** None
- **Last updated:** 2026-04-24

## Decisions

- 2026-04-13: Chose Option A (centered minimal layout) for welcome screen — clean list layout fits sidebar best, avoids scroll, native VS Code feel
- 2026-04-13: Removed stats, founder quote, and permissions disclosure — spec scoped to logo + tagline + 3 value props + CTA only
- 2026-04-13: Using Codicons (comment-discussion, star, organization) instead of emoji — follows design rules
- 2026-04-13: Logo using star SVG placeholder — final GitChat logo asset to be swapped in when provided
- 2026-04-14: Replaced SVG star placeholder with actual GitChat logo (media/icon.png) — added border + increased size to 56px
- 2026-04-14: Enhanced onboarding UI — animated RGB glow border, staggered entrance choreography, breathing icon glows, shimmer effects, smooth dismiss animation
- 2026-04-17: Developed feature to show full seen-by list (all users who have read the message, instead of just avatar stack) + fixed related seen receipts issues
- 2026-04-20: Continued fixing issues arising from the full seen-by list feature
- 2026-04-15: Fix #31/#32 Seen avatars wrong users (`gitchat-webapp/backend` + `gitchat_extension`) — backend emitted `conversation:read`/`reaction:updated` to room `conversation:<id>` but payload was missing `conversationId`; extension subscribes to multiple conv rooms simultaneously via `_subscribedConversations` Set → events leak between conversations. Client relay at `explore.ts:1695` only checked `if (_activeChatConvId)`, not `data.conversationId === _activeChatConvId`. 2-repo fix: (a) **BE** `messages.service.ts` added `conversationId` to 3 emit payloads (CONVERSATION_READ line 1245, REACTION_UPDATED add 1745 + remove 1797) — additive since `data: Record<string, any>`, backwards compatible with webapp frontend. (b) **Extension** 7 edits: `realtime/index.ts` types + parsers require `conversationId` or drop event; `explore.ts` 2 filter conditions; `sidebar-chat.js` 3 hardenings — (i) line 3661 remove `|| _state.otherReadAt` fallback leak between convs (2nd root cause of #32 "DM offline recipient shows seen" — client-only bug BE did not cover), (ii) line 3667 filter `readReceipts` init by `groupMembers`/`otherLogin`, (iii) line 3835 drop fallback `|| _state.otherLogin` and validate `readLogin` belongs to conversation. Required deploy order: BE first — shipping extension first would make `data.conversationId === _activeChatConvId` always false → seen avatar disabled entirely. Out of scope (follow-up issue): `sidebar-chat.js:3849-3862` marks all `.gs-sc-msg-out` as seen without comparing timestamp → messages sent after read get marked incorrectly.
- 2026-04-16: Fix #87 Discover tab empty-state flash (`gitchat_extension` FE-only) — root cause: `src/webviews/explore.ts:352-366, :388-402` already sent `stale: boolean` on every `setStarredReposData` / `setContributedReposData` payload but `media/webview/explore.js` handler dropped that field; render logic only had 3 branches (error / length===0 / has-items) → could not distinguish "fetching" from "actually 0 repos" → cold cache always flashed "Contribute to repos to join their teams" (and "Star repos..." for Communities) before fresh data arrived. Fix 5 edits in 1 file `explore.js`: (1) added `starredReposLoading = true`, `contributedReposLoading = true` state vars (init true); (2) reset both flags to true before each `fetchStarredRepos`/`fetchContributedRepos` dispatch when user enters Discover tab (fixes re-entry case); (3) handler derives `loading = stale && !error` — stale=true keeps loading, fresh or error turns loading off; (4) Teams render adds loading branch (error > loading > empty > all-joined); (5) Communities render pattern identical. Reused `.gs-empty gs-text-sm` + `codicon-loading codicon-modifier-spin` (already used in People search line 1050) — 0 new components, 0 new CSS. Rejected issue's Option B (BE waits for fresh before returning) because it would lose the benefit of stale-while-revalidate for users with warm cache. Rejected adding BE field `isLoading` — not needed because `stale` is sufficient signal. Edge case: if stale payload has items, render items immediately instead of hiding behind spinner (better perceived perf — loading only shows when `loading && length===0`). Verified manually in Extension Development Host (no webview JS test harness in repo).
- 2026-04-24: Drafted `docs/superpowers/specs/2026-04-24-repo-insight-design.md` — v1 scope Lean (install+videos+buzz, no Reddit/HN), 1 aggregate BE endpoint `GET /repo/:owner/:repo/insight` with `Promise.allSettled` partial-fail tolerance, Redis cache TTL by hotness tier (blazing 15m / hot 2h / warm 6h / cold 24h), new extension webview `RepoInsightPanel` triggered by existing unhandled `viewRepo` action. Scope out: Reddit/HN, AI summary, starring/forking — deferred to v2 based on telemetry. Spec includes 8 child-issue breakdown for GitHub.
- 2026-04-20: Fix #106 orphan ChatPanel zombie editor-tab (`gitchat_extension` FE-only) — root cause: after commit 71c7983 (12/04) sidebar became the sole chat surface, but `src/webviews/chat.ts` (class `ChatPanel` + `createWebviewPanel("gitchat.chat", ...)`) + legacy renderer `media/webview/chat.{js,css}` were never deleted, still bundled into `dist/extension.js` (~320KB). 0 callers in current source — tab only survives via VS Code hot-exit restoring tabs from pre-refactor sessions; no `WebviewPanelSerializer`, so VS Code renders stale HTML shell with the legacy renderer (missing embed/image/link-preview handlers that `sidebar-chat.js` has) → user saw messages "stripped of content" in editor tab while sidebar rendered them correctly. 2-part fix: (a) cleanup — deleted `chat.ts` + `chat.js` + `chat.css`, dropped `chatModule` import + 2 array entries in `extension.ts`, removed `ChatPanel.isOpen()` fallback in `notifications/index.ts` (sidebar is single source of truth), fixed stale comment in `chat-handlers.ts`; (b) defensive — `registerWebviewPanelSerializer("gitchat.chat", ...)` in `extension.ts:activate()` disposes zombie tab + redirects to sidebar via command `gitchat.openChat` if state has `conversationId`. Shared `chat-handlers.ts` kept (sidebar still uses it). Verified bug-report title "Chat: 👥 {GroupName}" was produced by `chat.ts:330` via `\u{1F465}` unicode escape (initial grep missed it due to encoding). Verification: type-check + lint + esbuild pass; post-fix bundle had 0 hits for `createWebviewPanel("gitchat.chat")` + 0 hits for `class ChatPanel` + 1 hit for new serializer; diff -7,759 / +21 lines. Low risk: 0 callers beforehand, sidebar untouched, reversible via `git revert`.
- 2026-04-24: Re-apply Fix #106 cleanup on `plan/channel_feature` — while auditing source in the socket/seen-message area, user reported many leftover files; investigation found commit `de6d187` from 2026-04-20 (original fix) **does not exist on any branch** (`git branch --contains de6d187` empty), only in reflog — likely dropped during some rebase/reset, so dead code returned. This round's diff matches the original commit + discovered 1 additional file: `media/webview/main.css` (2 lines, 0 references anywhere in source — was not part of the original 4/20 fix). Delete scope: `src/webviews/chat.ts` (511) + `media/webview/chat.js` (5151) + `media/webview/chat.css` (2230) + `media/webview/main.css` (2) = ~7894 lines. Edit scope: `extension.ts` (drop import + 2 array entries, keep `WebviewPanelSerializer` for zombie-tab dispose — still needed for hot-exit restore case), `notifications/index.ts` (drop `ChatPanel.isOpen` fallback, sidebar as single source), `chat-handlers.ts` comment. Verification: `npm run check-types` clean, `npm run compile` pass (8 ESLint warnings are all pre-existing in `realtime/nudge.ts` / `chat-group-messages.test.ts` / `explore.ts`, not caused by this diff). Not committed — user wants to review diff first. Suggested branching `cairo-cleanup-chatpanel` off `develop` rather than stacking on top of `plan/channel_feature` so the cleanup does not get mixed with the channel feature at PR time.
