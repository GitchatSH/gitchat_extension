# Ryan

## Current
- **Branch:** develop
- **Working on:** Killing assigned issues (#92, #76, #79, #56, #49, #35). Toast overhaul + WP8 Wave BE + WP10 Noti all shipped
- **Blockers:** None
- **Last updated:** 2026-04-16

## Decisions
- 2026-04-13: WP10 approach — native VS Code APIs (StatusBarItem + showInformationMessage) for cross-IDE compat (Cursor/Windsurf/Antigravity); inline Notifications section at top of Explore Chat tab; NO standalone webview bell panel
- 2026-04-14: WP10 UX overhaul — moved noti UI from inline-section + status-bar bell to a SINGLE source: native view title bar bell (next to new chat) → click opens a rich webview dropdown overlay with TODAY/YESTERDAY/EARLIER time grouping. Status bar bell removed entirely (was redundant). Title bar icon swaps between $(bell) and $(bell-dot) via gitchat.hasUnread context key. Auto mark-as-seen pattern: dropdown open clears unread badge but per-item dots stay until clicked (Linear pattern, via notificationStore.markAllSeen() local-only)
- 2026-04-14: WP10 UX v3 — switched from title-bar bell + popover to a 4th main tab "Noti" next to Chat/Friends/Discover. Rationale: mobile compatibility (title bar icons don't translate, tabs do). Removed title-bar menu entries, removed dropdown overlay HTML, kept all logic + rendering. New file pair notifications-pane.{css,js} replacing notifications-dropdown.*. Tab badge on "Noti" tab shows unread count. Auto mark-as-seen on tab open
- 2026-04-14: WP10 fixes from live prod testing — (a) filter notifications client-side to WP10's 5 types (mention/wave/follow/new_message/repo_activity), drop legacy types like event_like/post_reply/repo_starred that have no FE render and showed as 'nameless' rows. (b) Switch tab open from local markAllSeen to actual markAllRead — persists to BE so the badge stays at 0 across refreshes. Drop the seen-vs-read distinction; simpler model, matches user mental model
- 2026-04-14: WP10 repo_activity dual-schema render — handle both BE-4 shape (repoFullName/eventType/title) and legacy WP7 shape (repo_owner+repo_name/activity_type/activity_title). Humanize event types: 'pr_merged' → 'PR merged', 'commit_main' → 'commit to main', 'issue_opened' → 'issue opened'
- 2026-04-14: WP10 FE-7 Settings UI — per-type notification toggles in user menu Settings: Do Not Disturb, Mentions, Waves, New followers, Repo activity. Hydrate from BE inappPrefs on ready, persist via PUT /notifications/settings { inappPrefs: { [key]: value } }. apiClient signatures updated to expose inappPrefs in NotificationSettings. WP10 marked DONE in spec
- 2026-04-14: CI — added .github/workflows/notify-issues-telegram.yml to fire issue events into Telegram group https://t.me/c/3704701963/980 (chat_id -1003704701963, thread 980). Handles issues opened/closed/reopened/assigned/unassigned/labeled + issue_comment.created with random funny footers per event type. Reuses existing TELEGRAM_BOT_TOKEN; new secrets TELEGRAM_ISSUES_CHAT_ID + TELEGRAM_ISSUES_THREAD_ID. Skips Bot-authored events to avoid loops
- 2026-04-13: Delete zombie notification surfaces (webviews/notifications.ts + media assets) — never registered in package.json contributes.views; WP12 cleanup already removed tree-view and commands
- 2026-04-13: Unified unread badge on status bar — messages_unread + notifications_unread in one item, click → trending.openInbox
- 2026-04-13: Repo activity renders inline in WP7 community/team chat only, NOT as separate notification row (dedup with WP10)
- 2026-04-13: Mention persistence — BE persists notification row on @mention in messages (BE-2, previously WS-only), enabling reload-safe mention history
- 2026-04-13: CI — added Telegram push notify workflow to both gitchat_extension and gitchat-webapp, unified format via actions/github-script
- 2026-04-13: FE-5 inline noti list — isolated in notifications-section.js/.css loaded alongside explore.js (not embedded inside it) to survive WP4 Tab Layout rewrite by Hiru/Slug. Own window.message listener for 'setNotifications', own DOM subtree (#notif-section)
- 2026-04-13: WP10 BE all in gitchat-webapp@9264892: Wave module (waves table + routes), repo_activity fanout from GitHub webhooks (release/pr_merged/commit_main/issue_opened with 10min rate-limit), inapp_noti_prefs column + filter in createNotification
- 2026-04-14: Custom command gitchat.openNotifications — bell click in status bar opens sidebar + scrolls to noti section. Built-in workbench commands unreliable on Antigravity fork
- 2026-04-14: Toast throttle — max 1 active toast + 8s cooldown to prevent spam when many noti arrive at once
- 2026-04-14: viewAllNotifications QuickPick command — overflow when section has >5 items, supports search across actor/preview
- 2026-04-14: Bug fix in api.getNotifications() — BE TransformInterceptor returns flat shape when service response already has 'data' key, FE was double-unwrapping. Fixed by reading data.data directly without fallback chain
- 2026-04-14: UI redesign of notifications section — avatar 32px with type badge overlay (mention/wave/follow/repo/message colors), bold actor name, preview line, time ago, unread dot, uppercase header with pill, View all + Mark all read footer buttons. Uses --gs-* tokens to blend with conversation list
- 2026-04-15: Dead code cleanup — removed gitchat.viewAllNotifications + gitchat.markAllNotificationsRead command handlers and the notificationViewAll webview message case. Self-referential cluster: handler existed but no webview JS ever sent the message and no package.json contribution registered the commands. Verified via full repo grep
- 2026-04-15: WP8 Wave BE — shipped missing endpoints on gitchat-webapp develop (commits b730b0d..78058fb, 5 granular commits). (a) POST /waves/:id/respond with atomic side effects: creates/reuses DM, flips wave.status=accepted, marks wave notification read via new NotificationsService.markAsReadByWaveId helper, emits new WS event wave:responded to sender's user room with { waveId, conversationId, fromLogin, toLogin }. POST /waves/:id/accept kept as deprecated alias routing to same service method. (b) GET /waves/sent for FE to seed waved-set on boot. (c) New /modules/discover module with GET /discover/online-now — 5min lastSeenAt window, candidate pool 200, filters out self + mutual follows + already-waved + users with inappNotiPrefs.hideFromOnlineNow=true. Presence bootstrapped for free via NotificationsService.throttlePresenceUpdate() that ticks lastSeenAt on every getUnreadCount poll (~2min cadence). (d) Privacy flag hideFromOnlineNow toggled via existing PUT /notifications/settings inappPrefs path — no new route needed
- 2026-04-15: BE dead code cleanup (gitchat-webapp) — removed createOrGroupLikeNotification + createOrGroupStarNotification service methods and their sole callers in likes.service.ts (-174 lines across 2 commits). These wrote type='like'/'star' which were never in FOR_YOU_TYPES, so getNotifications filtered them out before reaching FE. Comments in likes.service.ts said "Send event_like notification" / "Send repo_starred notification" but the methods actually wrote the wrong type — latent bug that FE dropped explicitly on 2026-04-14 WP10 decision. createOrGroupFollowNotification left alone since 'follow' IS rendered by FE
- 2026-04-15: Doc contract drift caught — Hiru's WP8 BE requirements doc had several mismatches with actual BE shipped on 04-13: used target_login instead of toLogin, expected POST /waves/:id/respond as new (was shipped as /accept), status enum "responded" vs actual "accepted", error codes invented (mutual/blocked) that BE doesn't check. Message drafted for Hiru to sync spec with reality; BE ship prioritized not breaking already-shipped field names
- 2026-04-15: Toast UX overhaul — replacing 1-toast+8s-cooldown-drop throttle with Telegram-style per-conversation digest buffering. Urgent types (mention/wave) bypass buffer for individual toasts; chat messages accumulate into pending buckets per convo and surface as digest on next toast cycle instead of being silently dropped. New ToastCoordinator class in src/notifications/toast-coordinator.ts owns the drain loop
- 2026-04-15: Toast multi-convo digest format — when ≥2 conversations have pending messages, toast reads '5 new messages in 3 chats: latest: Alice — preview' with actions 'Open Inbox' (opens Noti tab) and 'Mark All Read' (bulk clears). Single convo keeps 'Alice (+3): preview' → 'Open Chat' click. Rationale: no single chat to route to, so surface totals + fast bulk ack
- 2026-04-15: Toast buffer dismissal on chat open — gitchat.openChat handler now calls toastCoordinator.clearConversation(convId) before navigating so the pending digest for that chat drops without firing. Prevents the stale 'Alice (+3)' toast from surfacing after the user already walked into that chat
- 2026-04-15: Toast burst cap — BUCKET_COUNT_CAP=20. Single-convo toasts above the cap show '(20+ new)' instead of '(+N)', multi-convo totals show '20+ new messages in K chats'. Prevents runaway counters when a chat floods (pasted log, etc.)
- 2026-04-15: Toast digest helpers extracted to src/notifications/toast-digest.ts as pure functions (addMessageToBuckets, formatSingleBucketDigest, formatMultiBucketDigest) so the fold/format logic can be unit tested without booting a VS Code host or mocking window APIs. Coordinator class stays thin around the drain loop
- 2026-04-15: Unit tests for toast-digest — 14 tests covering bucket fold semantics (create/increment/actor-refresh/Someone-fallback/per-convo isolation), single-bucket format (count=1/count>1/cap/beyond-cap/empty-preview), and multi-bucket format (latest-pick tie/latest-pick by count/cap label/empty-preview tail). Also fixed pre-existing broken test pipeline: pretest only ran check-types+lint+esbuild, never emitted compiled .test.js files, so 'npm test' was discovering 0 tests. Added compile-tests script (tsc emit) into pretest chain
- 2026-04-15: Filed issue #35 on gitchat_extension — chat message grouping doesn't reset on date boundary. Surfaced after test pipeline fix: chat-group-messages.test.ts 'date boundary resets group' fails with 'first' !== 'single'. Root cause suspected in sameSender/nextBreaks asymmetry — newDay check only on backward side. Routed to Hiru/Slug as chat UI owners
- 2026-04-15: Toast bug fix — isChatOpen detection only checked standalone ChatPanel.instances, never the sidebar explore view which is now the default chat surface. Result: toasts fired even while the user was actively reading the chat in the sidebar. Added ExploreWebviewProvider.isShowingChat(convId) that checks view.visible + _activeChatConvId match. handleIncoming now checks sidebar first, ChatPanel second
- 2026-04-15: Toast isShowingChat — dropped the vscode.window.state.focused gate after repro on Antigravity fork. VS Code forks (Antigravity, Cursor, Windsurf) can unset workbench focus when the webview gets interaction focus, making the gate fail even while the user is clearly in the chat. View visibility + active convId match is a more reliable signal. Trade-off: when VS Code is backgrounded to another app, toasts for the "currently active" conversation will be suppressed — acceptable since user is not watching the screen anyway and the badge + noti tab still catches up. Also added verbose enqueue logging so Dev Tools console surfaces queue state for future debugging
- 2026-04-15: navigateToChat same-convo guard — previously unconditionally called loadConversationData (apiClient.getMessages) on every open-chat click, even when the user was already on that conversation. Caused visible flicker + wasted API calls when tapping a notification for the chat you were already reading. Added sameConvo check: still posts chat:navigate to webview (so tab switches if user was on Friends/Discover/Noti), but skips the re-fetch. Realtime messages keep the view fresh anyway
- 2026-04-15: Duplicate toast source eliminated — root cause of stacked toasts even after the ToastCoordinator overhaul. statusBarModule.onNewMessage had its own direct vscode.window.showInformationMessage call bypassing the coordinator entirely, while notificationsModule.handleIncoming shows toasts via the coordinator. Backend fires both WS message:sent AND notification:new per chat message, so both pipelines fired in parallel → 2 toasts for every message, no digest/isShowingChat check on the statusBar side. Removed the toast block from statusBarModule entirely, kept only the badge-increment logic. statusBar now: onNewMessage → bump unread count + updateBadges. Notifications module owns every toast decision, single pipeline
- 2026-04-15: Vietnamese Enter-to-send fix — sidebar-chat.js wireInput had a 50ms post-compositionend debounce that swallowed every keydown, including Enter. Vietnamese Telex users committing diacritics then immediately pressing Enter saw newline instead of send. Fix: exempt Enter from the debounce — Enter is always a deliberate user action, never an IME artifact, so it is safe to process it even immediately after composition ends. Other text-producing keys still get the 50ms guard against IME stale-key bugs
- 2026-04-15: Issue #40 (Follow silent failure) fix — explore.ts profileCard:follow / :unfollow handlers swallowed BE errors (only logged + re-enabled the button via profileCardActionResult success:false). User had no idea why follow didn't work. Added surfaceFollowError() helper: extracts err.response.data.error.message, calls vscode.window.showErrorMessage. For error code GITHUB_FOLLOW_SYNC_FAILED (missing user:follow OAuth scope) the toast offers a "Sign In Again" action that runs gitchat.signOut → gitchat.signIn to refresh scopes. Same treatment for unfollow
- 2026-04-15: statusbar build break unblock — PR #37 (slug-qa-4) merged into develop and re-introduced the toast block I had removed in b1238a3, but the merge resolution kept my import removals (vscode, ChatPanel, mentionsSelf). Result: src/statusbar/index.ts referenced undefined symbols + had `await` in a non-async callback → tsc hard fail → npm:watch preLaunchTask blocked F5 dev host launch with "Errors exist after running preLaunchTask". Re-applied the cleanup: deleted the toast block again, kept only the badge fetchCounts() call. Notifications module remains the single owner of toast surfacing. Slug also added an exploreWebviewProvider.isConversationOpen() helper that duplicates my isShowingChat — left both for now, can dedupe later
- 2026-04-15: Issue #43 (Follow generic toast) fix — surfaceFollowError() now logs structured diagnostics to the output channel (status, code, beMsg, axiosMsg, dataType) so the next failure is debuggable without re-instrumenting. Toast fallback now appends HTTP status code or axios error code for visibility ("Failed to follow @X (HTTP 502)" or "(ECONNABORTED)"). Also bumped followUser/unfollowUser axios timeout from 10s → 20s — BE proxies through GitHub PUT /user/following/:username which can be slow on stale tokens, hitting ECONNABORTED before BE could surface a structured error
- 2026-04-15: Issue #43 followup — first round diagnostics confirmed status=502 but beCode/beMsg still '-'. Added raw err.response.data dump (first 500 chars) to the log so the next failing screenshot reveals the actual response body. Hypothesis: data is either a string (gateway HTML 502 from upstream) or a different envelope shape than { error: { code, message } }
- 2026-04-15: Issue #43 round 3 — output channel logs were not being read by the user. Inlined the data dump directly into the failure toast (Failed to follow @X (HTTP 502) | axiosMsg=... | data=...) and added console.error so the structured object surfaces in dev tools. Intentionally ugly UX, only meant to live until the actual response body is captured
- 2026-04-15: Issue #43 root cause — dump revealed response body is a Cloudflare/gateway HTML 502 page, NOT a BE JSON envelope. BE was never reached; Cloudflare returned its own error page because the origin (api-dev.gitchat.sh) is unreachable or the user's gitchat.apiUrl was not pointed at their local BE despite them testing "locally". surfaceFollowError now detects this case (data is a string starting with <html / <!doctype) and shows a dedicated toast: "GitChat server unreachable (HTTP 502). Check that the backend at {apiUrl} is running. If testing locally, override gitchat.apiUrl in settings." with "Retry" and "Open Settings" actions. Reverted the ugly data-dump fallback toast in non-gateway paths
- 2026-04-16: Issue #92 fix — wave toast showed '[object Object]' because FE read body.error (the entire {code,message,statusCode} envelope object) instead of body.error.message (the string). Unwrapped the BE envelope correctly. Also added 409 to terminal status list for WAVE_ALREADY_EXISTS and matched beCode in addition to beMsg for the terminal regex
- 2026-04-16: Issue #79 fix — mention toast in decideToast never checked isChatOpen, fired even when user was reading the conversation. Added the same isChatOpen guard that new_message already had
