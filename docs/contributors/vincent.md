# Vincent

## Current
- **Role:** BE
- **Working on (2026-04-20):** Noted new feature Topics inside Group/Team/Community chats — GitchatSH/gitchat_extension#148 (BE + FE at MVP level only). Support resolving newly arising issues, support deploy DEV + PROD env.
- **Blockers:** None
- **Last updated:** 2026-04-20

## Decisions
- 2026-04-17: Auto-admin Phase 3 (GitchatSH/gitchat_extension#123, `gitstar-webapp/backend`) — chose `GET /repos/:owner/:repo` permissions check (`permissions.admin || permissions.push`) over collaborator API (no special token needed, works with any user's OAuth). No cache — role persisted at join time into `message_conversation_members.role` column (varchar, default 'member'). Admin-mute uses separate table `message_conversation_muted_members` from self-mute (`message_muted_conversations`) — different semantics entirely. Role on rejoin (invite) NOT upgraded per spec (out of scope). FE not touched — owned by hiru/slugmarco.
- 2026-04-17: Wave auto-open DM (`gitchat_extension`) — after wave POST succeeds, immediately `executeCommand("gitchat.messageUser", login)` to navigate sender into the DM conversation where the "👋 X waved at Y" system message (from BE PR #22) is visible. Removed the info-message toast ("Waved at @X 👋") since opening the chat is a stronger confirmation signal. 1-line change in `explore.ts:1614`.
- 2026-04-17: feat(waves) create DM on wave send (`gitstar-webapp/backend`) — issue #101 reported sender sees no DM after waving. BE `sendWave` only created wave row + notification, no DM. `respondToWave` created DM but only on recipient accept. Fix: `sendWave` now calls `messagesService.createConversation` + `sendSystemMessage("👋 X waved at Y")` immediately. `createConversation` is idempotent (sorted p1/p2 + type='dm') so `respondToWave` still works without duplicating. Changed `sendSystemMessage` from private to public for cross-module access. Follow gate intentionally skipped (no userToken) since wave targets non-mutual users by design. FE side (#100 jank + #101 WS listener) on separate branch `vincent-fix-wave-ui-race`.
- 2026-04-17: Triaged + handed off #100 and #101 wave FE issues to hiru (unassigned Vincent). #100 is pure FE optimistic UI. #101 BE is by-design (DM on accept) but now changed to DM on send per product intent. #58 all done (PR #20 merged, role column flagged as future feature).
- 2026-04-16: Presence-unified v1 shipped across `gitchat_extension` + `gitchat-webapp/backend`. FE PR + BE PR opened against `develop`. Phase 1 ships unconditionally (stuck-offline fix, verified by user). Phase 2-5 (WS Online Now) ships behind `trending.wsDiscoverOnlineNow` flag default off. Flag-flip to default on scheduled as follow-up PR after 3-day dev-testing on api-dev. Phase 6 cleanup (remove REST fallback + deprecated `lastSeen` field) as separate follow-up PR one release later.
- 2026-04-16: Task 5.1 — Added `trending.wsDiscoverOnlineNow` flag (default off). REST is the default path; flag-on switches to WS subscription. Live-toggle via onDidChangeConfiguration.
- 2026-04-16: Task 4.2 — Wired Discover tab → WS discover:online-now with 3s REST fallback. Snapshot takes precedence over REST if it arrives late.
- 2026-04-16: Task 4.1 — RealtimeClient exposes discover online-now subscribe API + emitters; both channels write through to PresenceStore.
- 2026-04-16: Task 1.3 — hardened defensive nudge via `extractSenderLogin` helper (sender | senderLogin | author.login | from.login).
- 2026-04-16: Applied code-review feedback on PresenceStore (test + JSDoc).
- 2026-04-13: WP12 Cleanup — initially kept `repoDetailModule` (because it had UI refs), then user confirmed to delete it entirely. Removed all trending/feed/repo-detail/search webviews, tree-views, media assets. Kept `channel.ts` because the Channels tab is still live in the Explore UI.
- 2026-04-13: WP12 — do NOT rename `trending.*` command prefix and extension ID `top-github-trending`. Leave rebranding to WP1 — only delete dead code here.
- 2026-04-13: WP12 — need to restore hidden HTML shells (`#explore-header`, `#search-home`, `#search-results`) because `media/webview/explore.js` queries them unguarded; otherwise JS crashes and chat does not render.
- 2026-04-14: WP11 architecture — **backend proxy** instead of extension calling GitHub directly. Extension sends GitHub token via `Authorization: Bearer` header (existing pattern), BE proxies + caches server-side. Reduces round-trips for UI consumers, avoids burning the user's rate limit for the same data.
- 2026-04-14: WP11 3-tier cache — Redis hot (5 min) → Postgres persistent (24h via entity tables) → GitHub API. 5 min/user/data-type floor blocks `force=true` spam.
- 2026-04-14: WP11 caps — Starred 500, Contributed 100 (GitHub search/commits total cap 1000 results).
- 2026-04-14: WP11 contributed repos definition — use `/search/commits?q=author:LOGIN` to aggregate distinct repos. Simple enough, can upgrade to GraphQL `contributionsCollection` later if needed.
- 2026-04-14: WP11 friends — mutual follows computed via self-join on `user_follows` (existing table, already populated by `FollowingService.syncGitHubFollows`). No separate cache table created.
- 2026-04-14: WP11 profile — upsert into existing `user_profiles` table instead of creating a new one. Rich fields (bio, company, blog, twitter) fetched from `/users/{login}`.
- 2026-04-14: WP11 extension cache persisted in `context.globalState` for 24h, keyed by user login to prevent cross-account leaks. Cleared in `authManager.signOut()` before firing the event.
- 2026-04-14: WP11 — removed `apiClient.refreshAllGithubData()` call from `githubDataCache.refreshAll()` due to double-fetch race (both POST /refresh-all background job and 4 force GETs ran simultaneously → each data type fetched from GitHub twice). Kept only the 4 parallel force GETs. Endpoint `POST /github/data/refresh-all` left in place for future WebSocket push / scheduled refresh use.
- 2026-04-14: Fix #12 Copy Profile Badge — chose Option C (personalize badge URL → `dev.gitchat.sh/@<login>`) plus Option A (`menus.commandPalette` when `gitchat.isSignedIn`) + Option B (handler guard). Reason: command is named "Profile Badge" but the old string was a generic marketplace URL with no relation to the user's profile — copying it to a README was meaningless.
- 2026-04-14: Fix #13 View My Profile — preemptive guard with "Sign In" action button (fires `gitchat.signIn`), split error handling: signed-out uses info message, real errors use generic error message + log. Removed catch-all "Sign in first" because it was misleading for network errors. Added commandPalette gate. Did not abstract `requireSignIn()` helper yet since there are only 2 call-sites.
- 2026-04-14: Fix #13 bonus — `apiClient.getMyProfile()` was missing BE envelope unwrap (`data.data ?? data`) so it was broken even when signed in: `profile.login === undefined` → ProfilePanel opened `@undefined` then called `/user/undefined` returning 404. Fixed in the same PR since it was the same handler — not worth separating as a distinct bug.
- 2026-04-14: Fix #13 dead-code — removed `vscode.commands.executeCommand("gitchat.whoToFollow.focus")` from `auth/index.ts` because view `gitchat.whoToFollow` was removed during WP12 cleanup → rejected promise on every sign-in. 1-line removal, safe because the "Welcome" message still works.
- 2026-04-14: Fix #15 Discover Join Community — 3-layer bug: (1) webview sent `joinCommunity` instead of `chat:joinCommunity` → fixed prefix. (2) `explore.ts onMessage` gate `if (_activeChatConvId)` silently dropped the message when no chat was open → added special-case for `joinCommunity`/`joinTeam` before the gate with minimal ctx (handler does not use `ctx.conversationId`). (3) webview message handler for `chat:joinedConversation`/`chat:joinError` only reset `.tr-*-btn`, never touched `.discover-join-btn` → extended error handler to revert text "Join" + re-enable; success keeps optimistic state since the row has already navigated away. Original issue spec only covered layer 1, missing layers 2 and 3 — AC could not pass if only layer 1 was fixed.
- 2026-04-14: Fix #23 Discover search — search in Discover was entirely client-side (only filtered `chatFriends`/`chatChannels`) so any unfollowed user always returned "No results". Fix: added message `discoverSearchUsers` → handler calls `apiClient.searchUsers(query)` (already existed), posts `discoverSearchUsersResult` back. Webview: 300ms debounce (same pattern as `searchInboxMessages`), merges API results into PEOPLE section deduped by login, loading state shows "Searching…", empty state only triggers after API settles to prevent "No results" flash while loading. Also fixed Discover input placeholder from "Search channels..." → "Search people..." (matches intent). Communities search remains local-only (out of scope, separate issue if needed).
- 2026-04-14: Fix #23 bonus — clicking a search-result user was rejected by BE as "must follow on GitHub first" (because BE requires a GH follow to enable DM, per WP11 friends pattern). Fix: tag user from API with `_isSearchResult`, render row with `data-search-result="1"`, click handler branches: followed users → `chatOpenDM` as before, search results → `chatOpenProfile` → `ProfilePanel.show(extensionUri, login)`. User can follow from the profile then message afterward. Import `ProfilePanel` into `explore.ts`. No BE change — this is a FE UX gap, fixed in the right place. Scope creep but user approved since it's the same issue flow and separating it would be unreasonable.
- 2026-04-14: Fix #23 BE desync (first attempt, REVERTED) — tried a local-first check in `GitHubGateService.isFollowing`. User rejected: wanted GH fetched directly so GitHub is the source of truth, and wanted the root cause fixed at the write path rather than patching the read path. Reverted.
- 2026-04-14: Fix #23 Follow feature root cause (`gitstar-webapp/backend` + `gitchat_extension`) — desync `local_db vs github` caused by write path `FollowingService.followUser`: (1) upserts local DB first, (2) best-effort `PUT /user/following/:username`, (3) swallows error. PUT needs OAuth scope `user:follow` which the extension had not configured (`src/auth/index.ts` line 8: `["read:user", "user:email", "public_repo"]` — missing `user:follow`) → PUT returns 404 → `github_synced: false` but BE still returns success → local and GH are desynced → read path (unchanged, hits GH directly) rejects DM. **Fix 3 parts:** (a) extension scopes add `user:follow` (breaking: existing users must sign out/in to re-auth with the new scope). (b) BE `followUser` reverses order: GH first → if it fails throw `GITHUB_FOLLOW_SYNC_FAILED` (new error 502) with a clear reason (404 → "missing user:follow OAuth scope (sign out and sign back in)"), only upsert local when GH succeeds → invariant "local is never ahead of GH". (c) Same pattern for `unfollowUser`: GH DELETE first, local delete after. FE profile panel already has try/catch around `apiClient.followUser` → will show error toast correctly without further changes. Read path `isFollowing` not fixed — kept GH-direct as user requested.
- 2026-04-15: Discovery Community rewritten as client-side `starred ∖ joined` filter using existing `GET /github/data/starred` + `GET /channels`. Fix during testing: also read `chatConversations` community entries because community join writes to `message_conversation_members`, not `repo_channels` — reading only `chatChannels` leaves just-joined repos visible. No backend changes — avoided a backend PR to keep blast radius small.
- 2026-04-15: Triage #25 View Profile — closed as not reproducible. Re-traced every `doAction("viewProfile")` call site on current `develop`: `.search-person-item` dead (Chat search returns messages now), `.friend-profile-btn` selectors match zero elements in both `explore.js` and `chat-panel.js`, `.tp-card` dead (Trending tab removed + `#people-list` DOM doesn't exist), `chat.js` entirely dead (`ChatPanel.show` has no call sites — confirmed in profile-split design doc), user menu + `.gs-sc-mention` superseded by `window.ProfileCard.show` / `ProfileCard.bindTrigger` in-webview. Notifications tab not expected to open profile. Dead-code cleanup of `case "viewProfile"` handlers deferred to profile-split plan to avoid merge churn.
- 2026-04-15: Fix gitchat-ios-native#3 Follow notifications missing in Activity tab (`gitchat-webapp/backend`) — Arthur's previous fix (commit `877ecfa`) wired `syncGitHubFollows` to fire `createOrGroupFollowNotification` per new follower fire-and-forget, but Conal reopened because only 1 of 3 new followers appeared. Root cause is **two stacked bugs**: (1) `createOrGroupFollowNotification` groups every follow notification within a 1-hour window into a single row with `metadata.followers[]`, but both iOS (`NotificationsView.swift:139` → `"\(actor_login) followed you"`) and extension (`notifications-pane.js:68`) renderers only read `actor_login` and ignore the followers array — collapsing N followers into 1 invisible row. (2) Parallel fire-and-forget calls race on the `existing.metadata.followers` read-modify-write path, causing lost updates. Fix: in the sync bulk path only, replace `createOrGroupFollowNotification` with a direct `createNotification` (individual, non-grouped, `metadata: { source: 'github_sync' }`) — each new follower gets its own row, every renderer shows every follower, and parallel calls become trivially safe since there's no shared mutable state. Kept `createOrGroupFollowNotification` on the in-app `followUser` path: grouping still makes sense there in theory even though it has the same renderer UX issue + race, but that's a design decision for UX team and out of scope for #3. Not touching client renderers — the fix works with current iOS build v1.0.0-b39+ without needing a new app release.
- 2026-04-15: Fix #57 New Group modal missing mutual friends (`gitchat-webapp/backend`) — root cause in `github-friends.resolver.ts:71` where `onGitchat = !!p && !!p.lastSignInAt`. Column `last_sign_in_at` was added recently by migration `1775800000000-AddSignInTracking` (nullable, no backfill), so every user who signed in before that migration ran has `last_sign_in_at = NULL` until they sign in again — silently dropping historical accounts from the `mutual` bucket into `notOnGitchat`, which the FE New Group modal filters by `.onGitchat`. Issue spec guessed the fix was "use `.following` instead of `.mutual`" but the API has no `following` field and FE filter is redundant with BE — real bug is the proxy criterion. Changed to `!!p && p.githubId != null`: `github_id` is set by every auth flow (findOrCreateUser + touchSignIn backfill path) including historical users, and explicitly NOT set by the DM-side auto-provisioning upsert in `messages.service.ts:557-571`, so it cleanly partitions "authed Gitchat user" from "auto-created placeholder". Apple-only users have synthetic `apple-*` logins not present in `user_follows`, so they can't appear here regardless. No FE change, no migration, no API shape change — single-line BE fix. Not touching the sibling pattern in `github-profile.fetcher.ts:80` (same proxy, different feature surface) to keep PR scoped.
- 2026-04-15: Fix #51 Persistent message cache + SWR on chat reopen — new `src/services/message-cache.ts` wrapping `globalState` with per-user keying (`gitchat.msgCache.v1.<login>.<convId>`), 30 msgs × 20 convs cap, 7-day TTL, LRU eviction. Two-phase render in `loadConversationData`: on cache hit, post `chat:init` with `fromCache: true` (messages only, no header/metadata) so webview renders immediately with skeleton stripped; fetch continues in background and posts `chat:refresh` which reconciles metadata + full re-renders messages on divergent ID sets (noop on identical). Realtime `onNewMessage` hook appends to cache so out-of-session deltas persist for next SWR open. `signOut()` purges cache per-user to prevent cross-account leak. Decided against per-message diff/append animation — full re-render on divergence is simpler and there's no skeleton flash since skeleton is already detached. Header is painted from client-side `convData` at `SidebarChat.open()` time, so the cache-first init deliberately skips `renderHeaderFromInit` (which would overwrite correct header with empty placeholder) — refresh re-applies real metadata. No backend changes — pure FE caching, not expanding scope into WP11 territory.
- 2026-04-15: Fix #50 Chat skeleton not filling — two stacked bugs. (1) `.gs-sc-skeleton` had `justify-content: flex-end` with hardcoded 5-bubble pattern (~300px) → pushed to bottom, top half empty on typical viewports. Changed to `flex-start` and expanded pattern to 12 bubbles (stagger 0.08s instead of 0.12s to keep total fade-in snappy). (2) CSS class name collision: `.gs-sc-skel-out` was used both for side='out' bubble rows AND the fade-out transition class — cascade merged `opacity: 0` into all outgoing rows, making them invisible from render. Renamed fade-out class to `.gs-sc-skel-fading` + updated the single `classList.add` call site. Scope kept minimal: no changes to cross-fade timing, messages layout, keyframes, or row HTML structure.
- 2026-04-14: Workflow rules — pushed 2 new sections into root [CLAUDE.md](../../../CLAUDE.md): (1) "Issue-Fix Workflow" (fetch issue → trace code → guide repro → plan → approve → implement → user test → commit+PR → follow-up issues) to standardize how issues are handled. (2) "Personal Contribution Log (Vincent)" requires logging all work including BE/DevOps/data in `gitstar-webapp` into this `vincent.md`, because this is where the team looks to see contributions. This rule is cross-project so it belongs in the root rather than a sub-CLAUDE.md.
- 2026-04-15: Presence extension integration (`gitchat_extension`) — root cause of "socket not working" in local test: `src/realtime/index.ts:252` heartbeat emitted literal `"ping"` (pong roundtrip only) instead of the new `presence:heartbeat` backend event. After 90s the sweeper automatically marks the user offline. Fixes at 5 points: (1) `realtime/index.ts` emits `presence:heartbeat` every **30s hardcoded** (NOT using `presenceHeartbeat` config 60s because it is borderline with TTL 90s) + adds listener `presence:snapshot` (routes through same emitter as `presence:updated`) + extends emitter type with `lastSeenAt` + adds `WS_SUBSCRIBE.UNWATCH_PRESENCE` constant. (2) `api/index.ts` defines `PresenceEntry` interface, `getPresence` parses new shape `{ data: { login: { status, lastSeenAt } } }` instead of `{ presence: { login: ISO } }`, **removes `sendHeartbeat` HTTP method** (endpoint `PATCH /api/presence` was removed from backend). (3) `auth/index.ts:230` removes call to `apiClient.sendHeartbeat()` (dead caller). (4-5) `webviews/chat-panel.ts` + `webviews/explore.ts` (2 sites): old callers used staleness threshold heuristic `Date.now() - lastSeen < presenceHeartbeat * 5` to infer online status — now backend is authoritative, using `entry.status === 'online'` + `entry.lastSeenAt` directly for display. Build clean (tsc + eslint + esbuild), 3 lint warnings pre-existing and unrelated. Test: requires manual testing with backend `vincent-presence-redesign` running locally (WS port 3001 per dev override). package.json + src/config/index.ts contain user's localhost URL overrides — not committed.
- 2026-04-16: Presence online-dot fix round 3 (safety net) (`gitchat_extension`) — previously decided NOT to mark sender online on message receive ("backend is source of truth, client inference = drift"). Reversed after round 2 still left a window where a message arrives but dot stays gray (e.g. watch:presence ack in-flight, or sender dropped from slice(0,50) cap). Sender is definitively online if they just emitted — no false-positive risk. Added synthetic `_onPresence.fire({ user: sender, online: true, lastSeenAt: now })` inside the `MESSAGE_SENT` handler in `realtime/index.ts`, guarded against `sender === authManager.login`. Backend remains authoritative for offline transitions (only it can detect disconnect); client only ever nudges toward online. Fires the same event stream as real `presence:updated`, so both sidebar `chat:presence` passthrough and `debouncedRefreshChat` converge naturally. Build + lint clean.
- 2026-04-16: Presence online-dot fix round 2 (`gitchat_extension`) — first round (add `watch:presence`) fixed Discover/Friends list but Chat-list DM rows stayed inverted. Two additional bugs found: (A) `conversations.other_user.online` is a STATIC snapshot attached by backend at `GET /conversations` time — `explore.js:1579-1580` / `explore.js:1571-1584` short-circuits on `other_user.online !== undefined` without falling back to the live `friends` map, so `presence:updated` refreshes `chatFriends` but DM rows never re-read. (B) DM partners outside the user's `following` list are not in the slice passed to `getPresence`/`watchPresence`, so their presence is never fetched nor subscribed at all — `other_user.online` stays at whatever the backend wrote. Fix: in both `explore.ts` and `chat-panel.ts` `refreshChat` / refresh loops, after `getConversations()` compute DM partner logins missing from `presenceData`, do a supplementary `apiClient.getPresence(extra)` + `realtimeClient.watchPresence(extra)` (dedup + slice(0,50)), then when building `convData` overwrite `other_user.online` with live `presenceData[other.login].status === "online"`. Keeps backend unchanged. Still a known partial-coverage edge case: following > 50 + DM partners > 50 → later entries never subscribe (leads to "100 people all offline" report); needs batched presence endpoint or POST body variant (out of scope for this fix). Build + lint clean.
- 2026-04-16: Task 1.2 Unified Presence — added `src/realtime/presence-store.ts`: `PresenceStore` class as single source of truth for presence state, LRU-capped (default 1000) via Map insertion-order, `onChange`/`onEvict` VS Code EventEmitters, dedup on unchanged writes (refreshes LRU position without firing), `bulkSet`, and `snapshot()` returning a new object via `Object.fromEntries` (not the internal Map ref). Eviction only fires when a brand-new login pushes size above cap — existing-key writes refresh position without triggering eviction. Added unit tests at `src/test/suite/presence-store.test.ts` (Mocha TDD suite, 6 cases covering get/set, onChange fire, dedup, bulkSet, snapshot isolation, LRU eviction). Placed tests flat under `test/suite/` (not `test/realtime/`) because `src/test/suite/index.ts` harness only loads flat `*.test.js`. Wrote failing test first → confirmed `Cannot find module` → implemented → `npm run compile-tests` clean. Did not run full `npm test` (vscode-test requires GUI/display).
- 2026-04-16: Presence online-dot fix (`gitchat_extension`) — friends appeared permanently gray even though messages arrived in realtime. Root cause: `src/realtime/index.ts` defines `WS_SUBSCRIBE.WATCH_PRESENCE` but zero call sites emit it — grep confirmed. Backend `websocket-relayer.service.ts:249` only broadcasts `presence:snapshot`/`presence:updated` to clients that have joined the per-login presence room via `watch:presence`. Without the emit, `friend.online` stays frozen at the one-shot `GET /presence` value forever. Messages arrive via the orthogonal `subscribe:conversation` → `message:sent` path, which is why the symptom looks asymmetric. Fix: added `watchPresence(logins)` / `unwatchPresence(logins)` to `RealtimeClient`, emitting **per-login** `{ login }` (verified against backend `watchPresence` handler signature — NOT batched `{ logins: [] }`). Tracked logins in `_watchedPresenceLogins` Set and re-emitted inside the `connect` handler so watches survive reconnects (same pattern as `_subscribedConversations`). Cleared in `disconnect()`. Call sites: after each `apiClient.getPresence(logins.slice(0,50))` in `explore.ts` (2 places: Chat tab + Discover) and `chat-panel.ts` (1 place), call `realtimeClient.watchPresence(sameSlice)`. Subscription is idempotent (only emits for not-already-watched logins). Deliberately NOT touching backend and NOT adding a "mark sender online on message receive" fallback — backend is source of truth, client-side inference causes drift. Scope limited to first 50 logins (existing `getPresence` URL-length cap); partial coverage for larger friend lists is a pre-existing limit, not expanded here.
- 2026-04-16: Extend #86 fix to sidebar chat (`gitchat_extension`) — tester reported that in the sidebar flow (ExploreWebviewProvider + `media/webview/sidebar-chat.js`) the member count and group-info panel still stayed stale on member add/leave until clicking out to the main chat list. Original #86 fix only wired `ChatPanel` (popout), not the sidebar. Fix: subscribe `realtimeClient.onMemberAdded` / `onMemberLeft` in `src/webviews/explore.ts` module activate block, guard on `_activeChatConvId`, call `apiClient.getGroupMembers` and post `chat:membersUpdated` to the webview. In `sidebar-chat.js` `handleMessage` added a `'membersUpdated'` case that updates `_state.groupMembers`, refreshes `_els.headerSub`, and rebuilds `.gs-sc-gi-count`, `.gs-sc-gi-section-header`, and `.gs-sc-gi-members` in-place — reuses the same rebuild pattern from the existing Save-modal handler at line 5174+ (inlined rather than extracted to keep the PR scoped). Remove-button handlers re-bound after rebuild so admin can still remove without reopening the panel.
- 2026-04-16: Presence system redesign — client `PresenceStore` as single source of truth, new WS events `discover:online-now:*` replacing polling. Reusing existing BE `DiscoverService.getOnlineNow()` + `inappNotiPrefs.hideFromOnlineNow`. No new DB migration.
- 2026-04-16: Add-members modal in ChatPanel (`gitchat_extension`) — ChatPanel's existing `+ Add Member` button just toggled a minimal inline API-search list inside the group-info panel (no pills, no Save, no local-friends pre-fill), whereas sidebar-chat has a full "Members (N/50)" modal with mutual-friends + debounced API search + pills + Save. For parity and tester UX, ported `showEditMembersModal` → `showAddMembersModal` into `media/webview/chat.js` (uses module-local `groupMembers`/`currentUser`/`createdBy` instead of sidebar-chat's `_state`) and added a `fetchMutualFriendsFast` case in `chat-handlers.ts` mirroring `explore.ts:1309` (reuses `apiClient.getMyFriends(false)` + `mutual.onGitchat` filter, posts `mutualFriendsData`). Clicking `+ Add Member` now triggers the fetch → modal renders with pre-selected current members locked + mutual friends list + search; Save fires `addMember` per new login and relies on the #86 `member:added` → `membersUpdated` realtime path to refresh the panel in-place. Loaded `sidebar-chat.css` into ChatPanel webview to reuse the `.gs-sc-newchat-*` styles (zero collisions in `chat.css`/`chat.js`). Old inline `gip-search` kept as dead-ish fallback wired only if `groupSearchResults` arrives without an open modal — can remove in a later cleanup.
- 2026-04-16: Fix #86 realtime member-added UI staleness (`gitchat_extension`) — BE `member:added` payload already carries `{conversationId, login, addedBy}` but FE handler at `src/realtime/index.ts:171` only fired void `_onConversationUpdated` → triggered `debouncedRefreshChat` for the explore list, but the chat webview's `groupMembersList`/`_currentParticipants`/group-info panel were seeded once at `chat:init` and never re-populated. Fix FE-only in 3 touches: (1) `realtime/index.ts` add typed `onMemberAdded` / `onMemberLeft` emitters parsing `payload.data`, still firing `_onConversationUpdated` for the explore list path. (2) `webviews/chat.ts` `ChatPanel` subscribe to both events, guard on `data.conversationId === this._conversationId`, call `apiClient.getGroupMembers(convId)` (fresh fetch, no caching), post new `membersUpdated` message to webview. (3) `media/webview/chat.js` handle `membersUpdated`: overwrite `groupMembersList`/`groupMembers`/`_currentParticipants`, call `renderHeader(...)` to refresh "N members" subtitle, re-invoke `showGroupInfoPanel()` if the panel is currently open. No BE change — payload already sufficient and `GET /messages/conversations/:id/members` returns fresh `{login, name, avatar_url}`. Not touching `chat-handlers.ts` `groupInfo` (already re-fetches on open) or `explore.ts` (existing `onConversationUpdated` → `debouncedRefreshChat` → `getConversations()` path already refreshes the list member count). Related #84 (system message "X joined" missing) left out of scope — same BE flow but different surface.
- 2026-04-16: Fix GitchatSH/gitchat_extension#58 Part B + answer Part A (`gitstar-webapp/backend`) — issue is `[Confirm]` (phase 2, priority: low) with 4 BE questions + an "0 members" symptom. Investigation: (1) `message_conversation_members` entity has NO `role` column → no admin/owner/moderator concept exists today; FE `RepoChannel.role`/`ChannelMember.role` are vestigial. Adding roles is a future feature (schema migration + BE policy + FE wiring) — flagged as separate issue, out of scope. (2) Issue mis-attributes "0 members" to group create, but `createGroupConversation` (messages.service.ts:2473,2576) already enriches `conv.participants` with creator+others. Real bug is in `createCommunityOrTeamConversation`: returns `convEntityToConversation(conversation)` directly without populating `participants`, so FE reads `participants.length === 0` for fresh community/team joins. (3) Community auto-creation on first join: yes, `createdBy = login` is set (messages.service.ts:2627) but no role assignment because no role concept exists. Fix: mirror group-create enrichment — after the fresh-join WS emit branch, call `getActiveMembers(conversation.id)` + batch `userProfileRepository.find({ login: In(memberLogins) })` and build `conv.participants`. Same pattern as line 2570-2583 to keep style consistent. Build clean; lint noise pre-existing in file (not introduced). Did NOT add a `role` column / migration (separate design decision needs PM + hiru). Did NOT touch FE (hiru/slugmarco's surface). Posted answers on #58 + linked PR for Part B.
- 2026-04-16: Fix #94 phantom pinned-conversation limit (`gitstar-webapp/backend`) — user with 2 visible pins hit 400 "Maximum 3 pinned conversations". Root cause: only `deleteConversation` (DM soft-delete) cleaned up pins; `leaveGroup` / `removeMember` / `disbandGroup` all left orphan rows in `message_pinned_conversations`, which count queries picked up even though UI filtered them out (list join via members/deleted-convs). Fixed at app layer — added `unpinConversation` call in `leaveGroup` (for leaver) and `removeMember` (for target), plus bulk `messagePinnedConversationRepository.delete({ conversationId })` in `disbandGroup` after status flip. One-shot cleanup migration `1776900000009-CleanupOrphanPinnedConversations` deletes existing orphans across 4 buckets (left, soft-deleted, disbanded, dangling). Deliberately did NOT add FK/cascade to the entity — that's a schema concern worth its own issue (would need data migration review + backfill). Did NOT touch FE per updated root CLAUDE rule (BE default scope = `gitstar-webapp/backend/`, frontend is hiru/slugmarco). Did NOT extend fix to `message_pinned_messages` — same pattern may exist but out of scope, flagged as potential follow-up issue.
- 2026-04-16: Workflow rules hardening (root [CLAUDE.md](../../../CLAUDE.md)) — added "Hard rules" block at top of Issue-Fix Workflow: never auto-commit (Vincent commits manually even after verify), always pre-check if already resolved before coding, always checkout new feature branch, always ask before push/PR, always update this file in the same flow. Also added default-scope rule for BE tasks (`gitstar-webapp/backend/`, no frontend). Rationale: lock in the actual working pattern so future sessions don't drift into auto-commit or cross-project scope creep.
- 2026-04-15: Presence redesign (`gitstar-webapp/backend`) — QA reported online/offline feature was erratic, blocking Wave. Root cause: 6 bugs: (1) multi-tab race where `handleDisconnect` broadcast offline when closing 1 tab, (2) no server timeout on crash, (3) state split across 3 places (in-mem map, DB, FE), (4) broadcast/DB inconsistency, (5) online spam on reconnect, (6) zero tests. **Architecture decision:** Redis as source of truth. 4 data structures: `presence:sockets:{login}` (SET ref-count), `presence:heartbeat` (ZSET score=now for sweep), `presence:online:{login}` (STRING TTL 90s for O(1) check), `presence:socket_owner:{socketId}` (reverse lookup for cross-instance). 5 atomic Lua scripts (attach/detach/refresh/expireIfStale/forceOfflineLogout). Single-node Redis assertion in `onModuleInit`. Sockets-set MUST have TTL (load-bearing — orphan entries after an instance crash would permanently block 1→0 transitions). **Heartbeat 30s / TTL 90s** via WS event `presence:heartbeat` (no more HTTP PATCH). **Sweep every 15s** with cutoff 75s, running in Scheduler service via `@Interval(15000)` (NOT `@Cron` because the repo's `@nestjs/schedule` does not enable seconds). **Breaking API contract:** `GET /api/presence` returns `{login: {status, lastSeenAt}}` instead of plain lastSeen, adds `DELETE /api/presence` (nuclear logout for all tabs — HTTP cannot identify socketId so per-socket is not possible). Removed `PATCH/POST /presence` from `misc`. **Cross-instance:** leverages existing `RedisIoAdapter` + `WebSocketEmitterService` (@Global, uses @socket.io/redis-emitter) — no RabbitMQ pipeline needed for presence (big simplification vs original plan, discovered in pre-flight PF8). **Degraded mode:** Redis down → return all offline + lastSeenAt from Postgres, no 503. **Snapshot race fix:** `watch:presence` joins room BEFORE reading snapshot (not the other way around) — prevents lost transitions. **Test strategy:** 26 unit tests (store 15, service 6, sweeper 5) with fake Redis simulation; integration tests skipped (Option A — `test/integration/` empty, build infra is a separate scope). 12 commits on `vincent-presence-redesign`, all behind 2-stage review (spec compliance + code quality) for the most critical task (store service). Next: manual smoke test before coordinating with extension dev on contract + opening PR.

- 2026-04-16: Task 1.4 — routed presence events through `PresenceStore`, removed legacy `_onPresence` emitter. All consumers migrated.
- 2026-04-16: Task 1.5 — auto-watch presence on any WS event with a login ref; wired LRU eviction → unwatchPresence.
- 2026-04-16: Task 1.6 — removed 50-DM-partner presence cap; seed `PresenceStore` from REST bootstrap so all views share state.
- 2026-04-16: Task 3.2 — FE REST consumer `getOnlineNow()` now returns `lastSeenAt: string | null` instead of `lastSeen`; maps each user with fallback `u.lastSeenAt ?? u.lastSeen ?? null` for graceful deprecation window.
- 2026-04-17: Fix #124 ghost data flash (`gitchat_extension`) — sidebar chat in `explore.ts` had 3 race conditions where async `loadConversationData` could post messages from a previous conversation into the current view. Root cause: `navigateToChat` sets `_activeChatConvId` then awaits `loadConversationData` which makes multiple async calls (getMessages, getConversations, getGroupMembers, getPinnedMessages) — if user switches conversation mid-flight, results post to the wrong view. Fix: added `_navGeneration` monotonic counter incremented on each `navigateToChat`. Passed as `gen` param to `loadConversationData` which checks `gen !== this._navGeneration` (via `isStale()` helper) at 3 points: before SWR cache-hit post, after getMessages resolve, and after all metadata fetches before the main postToWebview. Stale results are discarded. Cache update still runs unconditionally (so next SWR open is fresh). Standalone `ChatPanel` (`chat.ts`) unaffected — uses immutable `this._conversationId`. ~20 lines changed, zero blast radius outside the sidebar chat async path.
- 2026-04-17: Fix #117 auto-split long messages — FE-only fix in `sidebar-chat.js`. Added `MAX_MSG_LENGTH = 2000` constant + `splitMessage()` helper that splits at word boundaries (hard-cut fallback for no-space text). `sendMessage()` now splits content into chunks ≤ 2000 chars and sends sequentially with 200ms delay. First chunk carries reply context, attachments, and link preview; subsequent chunks sent as plain messages. BE unchanged — limit already correct at 2000 chars in `messages.service.ts`.
- 2026-04-17: Fix GitchatSH/gitchat_extension#130 Bug A DM creation error masking (`gitstar-webapp/backend`, PR #26 merged to develop) — `POST /messages/conversations` was throwing generic `BAD_REQUEST "Failed to create conversation"` for three distinct failure modes: (1) GH `/users/:login` rate-limit/privacy-error silently swallowed by `catch {}` then misattributed as NOT_FOUND, (2) `message_conversations` insert failures (FK/constraint) swallowed with warn-only log, (3) true benign race where re-find resolves it (kept silent). Generic 400 also defeated extension's follow-gate toast retry (only triggers on `/follow this user on GitHub/i`). Fix: 2 new error codes `CONVERSATION_CREATE_FAILED` (500) and `GITHUB_USER_FETCH_FAILED` (502), both carrying context (login, recipientLogin, reason). GH 404 now maps to `NOT_FOUND "not found on GitHub"`; non-404 GH errors surface `RATE_LIMITED` / actual reason. Insert error message is captured and logged into the fallback throw. Same pattern applied to `createCommunityOrTeamConversation`. Kept existing `NOT_ELIGIBLE` path unchanged so FE follow-gate UX still works. Bug B (leeknowsai-specific root cause) deferred until api-dev deploy surfaces the real code — will track as follow-up. Not touching `GitHubGateService.isFollowing` (already distinguishes 404 cleanly) or FE. No migration, no API shape change.
- 2026-04-20: Noted GitchatSH/gitchat_extension#148 — new feature "Topics inside Group/Team/Community chats". Scope confirmed with PM: **BE + FE at MVP level only** for this first pass (no threading polish, no notification/preference surface, no search integration yet). No code written today — just captured the assignment + scope bound before planning. Full brainstorm (data model for topics vs messages, whether topics are sub-conversations or tags on messages, membership inheritance from parent convo, realtime event shape, pagination) deferred to a dedicated planning session. Flagging early so hiru/slugmarco can coordinate FE shell when BE shape lands.
- 2026-04-20: Support day — triaged follow-up issues spun off from recent fixes (no single-issue deep dive), assisted team with deploy DEV + PROD env operations (env var sync, build/deploy orchestration on `gitstar-webapp/backend`). No code commits today; DevOps + support only. Logged here per root CLAUDE rule that BE/DevOps work belongs in this file so the team sees cross-surface contribution.
