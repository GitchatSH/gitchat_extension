# Vincent

## Current
- **Branch:** `vincent-presence-integration` (`gitchat_extension`) — extension client integration of new presence contract; parallel branch `vincent-presence-redesign` on `gitstar-webapp/backend` has the matching backend rewrite
- **Task:** Backend rewrite of online/offline presence. QC reported the existing implementation as "loạn" / erratic. New design: Redis-backed source of truth (sockets SET ref-count + heartbeat ZSET + TTL online key + reverse lookup), Lua scripts for atomic transitions, single-node Redis cluster guard, 30s heartbeat / 90s TTL via WS event, 15s sweeper via `@Interval` in Scheduler service, broadcasts direct via `WebSocketEmitterService` (Redis pub/sub — no RabbitMQ). Breaking API change: `GET /api/presence` new shape + new `DELETE /api/presence` (nuclear logout), old `PATCH`/`POST /presence` removed from `misc`. Cross-instance via existing Socket.IO Redis adapter.
- **Today's plan (2026-04-15):**
  - All 12 implementation tasks complete on feature branch, 26 unit tests passing (store 15, service 6, sweeper 5)
  - Lint + build green on every touched file
  - Next: manual smoke test (multi-tab, crash via stale heartbeat inject, DELETE /api/presence, degraded mode) with running dev stack
  - Then push branch + coordinate contract with extension dev before PR merge
- **Working with:** extension dev integrating the new WS contract (`presence:heartbeat`, `unwatch:presence`, `presence:snapshot`) — not yet kicked off
- **Blockers:** None
- **Last updated:** 2026-04-15

## Decisions
- 2026-04-13: WP12 Cleanup — giữ `repoDetailModule` ban đầu (vì có UI ref), sau đó user chốt xóa luôn. Gỡ toàn bộ trending/feed/repo-detail/search webviews, tree-views, media assets. Giữ `channel.ts` vì Channels tab vẫn live trong Explore UI.
- 2026-04-13: WP12 — KHÔNG đổi `trending.*` command prefix và extension ID `top-github-trending`. Để WP1 Branding lo — chỉ xóa dead code.
- 2026-04-13: WP12 — cần restore các HTML shell ẩn (`#explore-header`, `#search-home`, `#search-results`) vì `media/webview/explore.js` query unguarded; nếu không sẽ crash JS và chat không render.
- 2026-04-14: WP11 kiến trúc — **backend proxy** thay vì extension gọi GitHub trực tiếp. Extension gửi GitHub token qua `Authorization: Bearer` header (pattern có sẵn), BE proxy + cache server-side. Giảm round-trip cho UI consumer, tránh burn user rate limit cho cùng 1 data.
- 2026-04-14: WP11 cache 3 tầng — Redis hot (5 min) → Postgres persistent (24h via entity tables) → GitHub API. Floor 5 min/user/data-type chặn spam `force=true`.
- 2026-04-14: WP11 caps — Starred 500, Contributed 100 (GitHub search/commits tổng cap 1000 results).
- 2026-04-14: WP11 contributed repos định nghĩa — dùng `/search/commits?q=author:LOGIN` gom distinct repos. Đủ đơn giản, có thể upgrade sang GraphQL `contributionsCollection` sau nếu cần.
- 2026-04-14: WP11 friends — mutual follows compute bằng self-join trên `user_follows` (table có sẵn, `FollowingService.syncGitHubFollows` đã fill). Không tạo bảng cache riêng.
- 2026-04-14: WP11 profile — upsert vào `user_profiles` table có sẵn thay vì tạo bảng mới. Rich fields (bio, company, blog, twitter) lấy từ `/users/{login}`.
- 2026-04-14: WP11 extension cache persist ở `context.globalState` 24h, key theo user login để tránh cross-account leak. Clear trong `authManager.signOut()` trước khi fire event.
- 2026-04-14: WP11 — bỏ lời gọi `apiClient.refreshAllGithubData()` trong `githubDataCache.refreshAll()` vì race double-fetch (cả POST /refresh-all background và 4 force GET cùng chạy → mỗi data type fetch GitHub 2 lần). Chỉ giữ 4 force GET song song. Endpoint `POST /github/data/refresh-all` để lại cho WebSocket push / scheduled refresh sau.
- 2026-04-14: Fix #12 Copy Profile Badge — chọn Option C (personalize badge URL → `dev.gitchat.sh/@<login>`) kèm Option A (`menus.commandPalette` when `gitchat.isSignedIn`) + Option B (handler guard). Lý do: tên command là "Profile Badge" nhưng chuỗi cũ là marketplace URL generic, không liên quan profile user — copy ra README cũng vô nghĩa.
- 2026-04-14: Fix #13 View My Profile — preemptive guard với action button "Sign In" (fire `gitchat.signIn`), tách error handling: signed-out dùng info message, error thật dùng error message generic + log. Bỏ catch-all "Sign in first" vì misleading cho lỗi network. Thêm commandPalette gate. Chưa abstract helper `requireSignIn()` vì mới 2 call-sites.
- 2026-04-14: Fix #13 bonus — `apiClient.getMyProfile()` thiếu unwrap envelope BE (`data.data ?? data`) nên signed-in cũng broken: `profile.login === undefined` → ProfilePanel mở `@undefined` rồi gọi `/user/undefined` 404. Fix luôn trong cùng PR vì cùng handler, không hợp lý tách bug riêng.
- 2026-04-14: Fix #13 dead-code — gỡ `vscode.commands.executeCommand("gitchat.whoToFollow.focus")` trong `auth/index.ts` vì view `gitchat.whoToFollow` đã bị gỡ trong WP12 cleanup → rejected promise mỗi lần sign-in. 1-line removal, an toàn vì message "Welcome" vẫn hoạt động.
- 2026-04-14: Fix #15 Discover Join Community — 3 tầng bug: (1) webview gửi `joinCommunity` thay vì `chat:joinCommunity` → đổi prefix. (2) `explore.ts onMessage` gate `if (_activeChatConvId)` silently drop message khi chưa mở chat → thêm special-case cho `joinCommunity`/`joinTeam` trước gate với minimal ctx (handler không dùng `ctx.conversationId`). (3) webview message handler cho `chat:joinedConversation`/`chat:joinError` chỉ reset `.tr-*-btn`, không chạm `.discover-join-btn` → mở rộng error handler revert text "Join" + enable; success giữ nguyên optimistic state vì row navigate đi rồi. Issue spec gốc chỉ cover tầng 1, thiếu 2 và 3 — AC không thể pass nếu chỉ fix tầng 1.
- 2026-04-14: Fix #23 Discover search — search trong Discover hoàn toàn client-side (chỉ filter `chatFriends`/`chatChannels`) nên mọi user chưa follow đều ra "No results". Fix: thêm message `discoverSearchUsers` → handler gọi `apiClient.searchUsers(query)` (đã có sẵn), post `discoverSearchUsersResult` về. Webview: debounce 300ms (pattern giống `searchInboxMessages`), merge API results vào PEOPLE section dedup by login, loading state show "Searching…", empty state chỉ trigger sau khi API settled để tránh flash "No results" lúc đang load. Cũng sửa placeholder input Discover từ "Search channels..." → "Search people..." (match intent). Communities search vẫn local-only (out of scope, tách issue nếu cần).
- 2026-04-14: Fix #23 bonus — click vào search-result user bị BE reject "must follow on GitHub first" (vì BE cần GH follow để enable DM, pattern từ WP11 friends). Fix: tag user từ API bằng `_isSearchResult`, render row với `data-search-result="1"`, click handler rẽ nhánh: followed users → `chatOpenDM` như cũ, search results → `chatOpenProfile` → `ProfilePanel.show(extensionUri, login)`. User có thể follow từ profile rồi message sau. Import `ProfilePanel` vào `explore.ts`. Không đụng BE — là FE UX gap, fix đúng nơi. Scope creep nhưng user approve vì cùng issue flow, không hợp lý tách.
- 2026-04-14: Fix #23 BE desync (first attempt, REVERTED) — thử local-first check trong `GitHubGateService.isFollowing`. User reject: muốn fetch GH trực tiếp để GH là source of truth, và muốn fix đúng root cause ở write path thay vì patch read path. Reverted.
- 2026-04-14: Fix #23 Follow feature root cause (`gitstar-webapp/backend` + `gitchat_extension`) — desync `local_db vs github` do write path `FollowingService.followUser`: (1) upsert local DB trước, (2) best-effort `PUT /user/following/:username`, (3) swallow error. PUT cần OAuth scope `user:follow` mà extension chưa config (`src/auth/index.ts` line 8: `["read:user", "user:email", "public_repo"]` — thiếu `user:follow`) → PUT trả 404 → `github_synced: false` nhưng BE vẫn return success → local và GH desync → read path (giữ nguyên, hit GH direct) reject DM. **Fix 3 phần:** (a) extension scopes thêm `user:follow` (breaking: existing users phải sign out/in lại để re-auth với scope mới). (b) BE `followUser` đảo thứ tự: GH first → nếu fail thì throw `GITHUB_FOLLOW_SYNC_FAILED` (new error 502) với reason rõ ràng (404 → "missing user:follow OAuth scope (sign out and sign back in)"), chỉ upsert local khi GH success → invariant "local không bao giờ ahead của GH". (c) Tương tự `unfollowUser`: GH DELETE first, local delete sau. FE profile panel đã có try/catch quanh `apiClient.followUser` → sẽ show error toast đúng cách không cần sửa thêm. Không fix read path `isFollowing` — giữ GH-direct như user yêu cầu.
- 2026-04-15: Discovery Community rewritten as client-side `starred ∖ joined` filter using existing `GET /github/data/starred` + `GET /channels`. Fix during testing: also read `chatConversations` community entries because community join writes to `message_conversation_members`, not `repo_channels` — reading only `chatChannels` leaves just-joined repos visible. No backend changes — avoided a backend PR to keep blast radius small.
- 2026-04-15: Triage #25 View Profile — closed as not reproducible. Re-traced every `doAction("viewProfile")` call site on current `develop`: `.search-person-item` dead (Chat search returns messages now), `.friend-profile-btn` selectors match zero elements in both `explore.js` and `chat-panel.js`, `.tp-card` dead (Trending tab removed + `#people-list` DOM doesn't exist), `chat.js` entirely dead (`ChatPanel.show` has no call sites — confirmed in profile-split design doc), user menu + `.gs-sc-mention` superseded by `window.ProfileCard.show` / `ProfileCard.bindTrigger` in-webview. Notifications tab not expected to open profile. Dead-code cleanup of `case "viewProfile"` handlers deferred to profile-split plan to avoid merge churn.
- 2026-04-15: Fix gitchat-ios-native#3 Follow notifications missing in Activity tab (`gitchat-webapp/backend`) — Arthur's previous fix (commit `877ecfa`) wired `syncGitHubFollows` to fire `createOrGroupFollowNotification` per new follower fire-and-forget, but Conal reopened because only 1 of 3 new followers appeared. Root cause is **two stacked bugs**: (1) `createOrGroupFollowNotification` groups every follow notification within a 1-hour window into a single row with `metadata.followers[]`, but both iOS (`NotificationsView.swift:139` → `"\(actor_login) followed you"`) and extension (`notifications-pane.js:68`) renderers only read `actor_login` and ignore the followers array — collapsing N followers into 1 invisible row. (2) Parallel fire-and-forget calls race on the `existing.metadata.followers` read-modify-write path, causing lost updates. Fix: in the sync bulk path only, replace `createOrGroupFollowNotification` with a direct `createNotification` (individual, non-grouped, `metadata: { source: 'github_sync' }`) — each new follower gets its own row, every renderer shows every follower, and parallel calls become trivially safe since there's no shared mutable state. Kept `createOrGroupFollowNotification` on the in-app `followUser` path: grouping still makes sense there in theory even though it has the same renderer UX issue + race, but that's a design decision for UX team and out of scope for #3. Not touching client renderers — the fix works with current iOS build v1.0.0-b39+ without needing a new app release.
- 2026-04-15: Fix #57 New Group modal missing mutual friends (`gitchat-webapp/backend`) — root cause in `github-friends.resolver.ts:71` where `onGitchat = !!p && !!p.lastSignInAt`. Column `last_sign_in_at` was added recently by migration `1775800000000-AddSignInTracking` (nullable, no backfill), so every user who signed in before that migration ran has `last_sign_in_at = NULL` until they sign in again — silently dropping historical accounts from the `mutual` bucket into `notOnGitchat`, which the FE New Group modal filters by `.onGitchat`. Issue spec guessed the fix was "use `.following` instead of `.mutual`" but the API has no `following` field and FE filter is redundant with BE — real bug is the proxy criterion. Changed to `!!p && p.githubId != null`: `github_id` is set by every auth flow (findOrCreateUser + touchSignIn backfill path) including historical users, and explicitly NOT set by the DM-side auto-provisioning upsert in `messages.service.ts:557-571`, so it cleanly partitions "authed Gitchat user" from "auto-created placeholder". Apple-only users have synthetic `apple-*` logins not present in `user_follows`, so they can't appear here regardless. No FE change, no migration, no API shape change — single-line BE fix. Not touching the sibling pattern in `github-profile.fetcher.ts:80` (same proxy, different feature surface) to keep PR scoped.
- 2026-04-15: Fix #51 Persistent message cache + SWR on chat reopen — new `src/services/message-cache.ts` wrapping `globalState` with per-user keying (`gitchat.msgCache.v1.<login>.<convId>`), 30 msgs × 20 convs cap, 7-day TTL, LRU eviction. Two-phase render in `loadConversationData`: on cache hit, post `chat:init` with `fromCache: true` (messages only, no header/metadata) so webview renders immediately with skeleton stripped; fetch continues in background and posts `chat:refresh` which reconciles metadata + full re-renders messages on divergent ID sets (noop on identical). Realtime `onNewMessage` hook appends to cache so out-of-session deltas persist for next SWR open. `signOut()` purges cache per-user to prevent cross-account leak. Decided against per-message diff/append animation — full re-render on divergence is simpler and there's no skeleton flash since skeleton is already detached. Header is painted from client-side `convData` at `SidebarChat.open()` time, so the cache-first init deliberately skips `renderHeaderFromInit` (which would overwrite correct header with empty placeholder) — refresh re-applies real metadata. No backend changes — pure FE caching, not expanding scope into WP11 territory.
- 2026-04-15: Fix #50 Chat skeleton not filling — two stacked bugs. (1) `.gs-sc-skeleton` had `justify-content: flex-end` with hardcoded 5-bubble pattern (~300px) → pushed to bottom, top half empty on typical viewports. Changed to `flex-start` and expanded pattern to 12 bubbles (stagger 0.08s instead of 0.12s to keep total fade-in snappy). (2) CSS class name collision: `.gs-sc-skel-out` was used both for side='out' bubble rows AND the fade-out transition class — cascade merged `opacity: 0` into all outgoing rows, making them invisible from render. Renamed fade-out class to `.gs-sc-skel-fading` + updated the single `classList.add` call site. Scope kept minimal: no changes to cross-fade timing, messages layout, keyframes, or row HTML structure.
- 2026-04-14: Workflow rules — đẩy vào root [CLAUDE.md](../../../CLAUDE.md) 2 section mới: (1) "Issue-Fix Workflow" (fetch issue → trace code → guide repro → plan → approve → implement → user test → commit+PR → follow-up issues) để chuẩn hoá cách xử lý issue. (2) "Personal Contribution Log (Vincent)" bắt buộc log mọi work kể cả BE/DevOps/data ở `gitstar-webapp` vào `vincent.md` này, vì đây là nơi team pull để thấy contribution. Rule này là cross-project nên đặt ở root thay vì sub-CLAUDE.md.
- 2026-04-15: Presence extension integration (`gitchat_extension`) — bug root cause của "socket chưa work" trong local test: `src/realtime/index.ts:252` heartbeat emit literal `"ping"` (chỉ pong roundtrip) thay vì `presence:heartbeat` event mới của backend. Sau 90s sweeper tự động mark user offline. Fixes 5 chỗ: (1) `realtime/index.ts` emit `presence:heartbeat` mỗi **30s hardcoded** (KHÔNG dùng `presenceHeartbeat` config 60s vì borderline với TTL 90s) + thêm listener `presence:snapshot` (route chung emitter với `presence:updated`) + extend emitter type với `lastSeenAt` + add `WS_SUBSCRIBE.UNWATCH_PRESENCE` constant. (2) `api/index.ts` define `PresenceEntry` interface, `getPresence` parse shape mới `{ data: { login: { status, lastSeenAt } } }` thay vì `{ presence: { login: ISO } }`, **xóa `sendHeartbeat` HTTP method** (endpoint `PATCH /api/presence` đã bị xóa khỏi backend). (3) `auth/index.ts:230` xóa call `apiClient.sendHeartbeat()` (dead caller). (4-5) `webviews/chat-panel.ts` + `webviews/explore.ts` (2 sites): callers cũ dùng staleness threshold heuristic `Date.now() - lastSeen < presenceHeartbeat * 5` để suy ra online — giờ backend authoritative, dùng trực tiếp `entry.status === 'online'` + `entry.lastSeenAt` cho display. Build clean (tsc + eslint + esbuild), 3 lint warnings pre-existing không liên quan. Test: cần manual với backend `vincent-presence-redesign` chạy local (WS port 3001 per dev override). package.json + src/config/index.ts có URL localhost override của user — không commit.
- 2026-04-15: Presence redesign (`gitstar-webapp/backend`) — QC báo tính năng online/offline loạn, block Wave. Root cause 6 bug: (1) multi-tab race `handleDisconnect` broadcast offline khi đóng 1 tab, (2) no server timeout cho crash, (3) state split 3 nơi (in-mem map, DB, FE), (4) broadcast/DB inconsistency, (5) online spam trên reconnect, (6) zero test. **Quyết định kiến trúc:** Redis là source of truth. 4 data structure: `presence:sockets:{login}` (SET ref-count), `presence:heartbeat` (ZSET score=now cho sweep), `presence:online:{login}` (STRING TTL 90s cho O(1) check), `presence:socket_owner:{socketId}` (reverse lookup cho cross-instance). 5 Lua scripts atomic (attach/detach/refresh/expireIfStale/forceOfflineLogout). Single-node Redis assertion trong `onModuleInit`. Sockets-set PHẢI có TTL (load-bearing — orphan entries sau instance crash sẽ block vĩnh viễn 1→0 transitions). **Heartbeat 30s / TTL 90s** qua WS event `presence:heartbeat` (không còn HTTP PATCH). **Sweep 15s** với cutoff 75s, chạy trong Scheduler service qua `@Interval(15000)` (KHÔNG `@Cron` vì repo `@nestjs/schedule` không enable seconds). **API contract breaking:** `GET /api/presence` trả `{login: {status, lastSeenAt}}` thay vì plain lastSeen, thêm `DELETE /api/presence` (nuclear logout cho tất cả tabs — HTTP không định danh socketId nên không thể per-socket). Xóa `PATCH/POST /presence` khỏi `misc`. **Cross-instance:** tận dụng `RedisIoAdapter` có sẵn + `WebSocketEmitterService` (@Global, uses @socket.io/redis-emitter) — không cần RabbitMQ pipeline cho presence (big simplification vs plan gốc, phát hiện trong pre-flight PF8). **Degraded mode:** Redis down → trả all offline + lastSeenAt từ Postgres, không 503. **Snapshot race fix:** `watch:presence` join room TRƯỚC khi đọc snapshot (không phải ngược lại) — tránh lost transition. **Test strategy:** 26 unit tests (store 15, service 6, sweeper 5) với fake Redis simulation; integration tests skip (Option A — `test/integration/` rỗng, build infra là scope riêng). 12 commits trên `vincent-presence-redesign`, tất cả behind 2-stage review (spec compliance + code quality) cho task quan trọng nhất (store service). Next: manual smoke test trước khi coord với extension dev cho contract + mở PR.
