# nakamoto-hiru

## Current
- **Role:** FE
- **Branch:** `hiru-uiux` (synced with `origin/develop` — 4 ahead / 0 behind; local pending push)
- **Working on:** Finish **#133 Part 1** (QA + open PR to `develop`), then **resume #139** (PR #157 draft on branch `hiru-admin-role`).
- **Blockers:** None — BE fix for #133 merged (see below); waiting on deploy confirmation to `api-dev` before full QA matrix.
- **Last updated:** 2026-04-21

## Today's Plan (2026-04-21)

Sequential — finish #133 first (nearly done), then resume #139.

### Phase A — #133 Smart notification hybrid toast (Part 1)

**Starting state (already done, do NOT redo):**
- FE Part 1 code shipped on `hiru-uiux` as commit `39a30c5` (ToastRenderer router + NativeRenderer + WebviewRenderer + `toast-stack.{js,css}` + 13 unit tests). See `docs/superpowers/specs/2026-04-20-smart-notification-hybrid-delivery-design.md` + `docs/superpowers/plans/2026-04-20-smart-notification-hybrid-delivery.md`.
- BE fix `GitchatSH/gitchat-webapp#33` MERGED to `develop` at 2026-04-20 09:12 UTC (Vincent). `MessagesService.sendMessage()` now fans out `notification:new` with `type=new_message` to every online recipient (DM / group / team / community). Payload shape unchanged; FE routing already handles `type=new_message` → `gitchat.openChat(conversationId)` + `markRead`. Follow-up commit `fcc9a5f` hides `new_message` from Noti inbox (renamed `FOR_YOU_TYPES` → `INBOX_VISIBLE_TYPES`).
- `hiru-uiux` just pulled all 14 commits from `origin/develop` (includes `feat/opensource` env-vars refactor — `.env` required locally, created from `.env.example` with dev endpoints).

**Steps:**
1. **Verify BE deploy to `api-dev`** before QA. Ask @beenvincent or @norwayishere (2 builds/day cadence). If not deployed yet: wait or pin local `gitchat.apiUrl` to a local BE running `vincent-fix-new-message-notification`.
2. **Run Vincent's 6-scenario QA matrix** in Extension Development Host with 2 VS Code windows (A + B) both signed in:
   1. **Happy DM** — B sidebar visible + VS Code focused → A DM → B should see 280 px in-sidebar toast, 4 s auto-dismiss, click body → opens chat.
   2. **Group fan-out** — 3+ online members in a group → every recipient toast; stack caps at 3 cards visible.
   3. **Native fallback** — B collapses sidebar / unfocuses VS Code → A DM → B sees native VS Code info toast (not webview).
   4. **Offline** — B quits VS Code → A DM → no toast, only OneSignal push. Check group push heading ("A to groupname").
   5. **Pref gate** — flip `inappNotiPrefs.new_message = false` on B → repeat (1) → no toast, no DB row.
   6. **Mention coexistence** — A sends `@B hey` in a group → B sees two toasts (`mention` + `new_message`). Intended per PM; flag back if noisy in practice.
3. **DB sanity check (optional)** — run against `api-dev`:
   ```sql
   SELECT type, count(*) FROM notifications
   WHERE created_at > NOW() - INTERVAL '10 minutes' GROUP BY type;
   ```
   Expect `new_message` rows appearing per delivered online message.
4. **Open PR `hiru-uiux` → `develop`** — title `feat(notifications): hybrid in-app webview toast (#133 Part 1)`. Body: 1-paragraph summary + QA checklist (which of the 6 scenarios pass) + link to spec + plan. Close Part 1 of #133 with reference; keep issue OPEN for Part 2.
5. **File separate tiny issue** for timestamp `-19390s` FE formatter bug Vincent flagged, if reproducible in Noti inbox. Do NOT block #133 PR on it.
6. **Part 2 (out-of-app OS-level)** — remains a research spike, deferred. Not in scope this session.

### Phase B — #139 Admin role UI (Phase 3 FE)

**Starting state (already done, do NOT redo):**
- Branch: `hiru-admin-role` (remote `origin/hiru-admin-role` also exists).
- PR: `#157` OPEN **DRAFT**, base `develop` ← head `hiru-admin-role`, +2756 / −170, last updated 2026-04-20 11:31 UTC.
- Single commit `a7ac161` — `feat(#139): wire admin role UI — kick + timed mute (Phase 3 FE)`.
- Implemented: `ConversationParticipant.role?: "admin" | "member"`, new APIs `kickGroupMember` + `adminMuteGroupMember`, kebab-menu per member in Group Info (both `chat.js` + `sidebar-chat.js`), mute modal with 6 presets (1h/4h/8h/1d/3d/1w), error-code mapping (`NOT_ADMIN`, `CANNOT_KICK_ADMIN`, `CANNOT_MUTE_ADMIN`, `USER_MUTED`), shared `gs-btn-lg` polish + shared mute-modal styles in `shared.css`.
- **Blocked on BE (logged in PR #157 body):** Unmute action needs (a) `muted_until` exposed in `GET /members`, (b) `DELETE /admin-mute` endpoint. Vincent owns — not yet shipped.

**Steps:**
1. **Switch branch** — after #133 PR opened: `git checkout hiru-admin-role`, `git pull origin hiru-admin-role`.
2. **Rebase/merge `develop`** into `hiru-admin-role` (14-commit backlog from today's sync will apply here too — expect env-vars / `.env` setup already in place, but verify `npm run compile` passes post-merge).
3. **Ping Vincent on #139 / PR #157** for status of unmute BE endpoints. Two possibilities:
   - **BE ready** → wire unmute UI (read `muted_until`, call `DELETE /admin-mute`), mark PR #157 ready for review, request review from @slugmacro or @norwayishere.
   - **BE not ready** → keep PR draft, move on to next assigned issue (check `gh issue list --assignee @me --state open` for bug-clearing backlog per announcement 2026-04-17).
4. **Do not** re-implement kick / timed mute — that is done on `a7ac161`. Only layer unmute on top + polish if review feedback comes in.

### Working with
- @beenvincent — BE owner for #133 deploy confirmation + #139 unmute endpoints.
- @norwayishere — release cadence (2 builds/day) + PR review.
- Solo for FE work on both issues.

### Verification
- Both issues: `npm run compile` clean + manual smoke in Extension Development Host.
- #133: Vincent's 6-scenario matrix above.
- #139: admin vs non-admin view switching, kick happy/error paths, mute modal all 6 presets, error toasts for each BE error code.

### Key reference files (for future Claude check-ins)
- Spec: `docs/superpowers/specs/2026-04-20-smart-notification-hybrid-delivery-design.md`
- Plan: `docs/superpowers/plans/2026-04-20-smart-notification-hybrid-delivery.md`
- PR #157 URL: https://github.com/GitchatSH/gitchat_extension/pull/157
- BE PR #33 URL: https://github.com/GitchatSH/gitchat-webapp/pull/33 (merged)

## Decisions
- 2026-04-21: Session-start sync — merged `origin/develop` into `hiru-uiux` (14 commits, no conflicts; `explore.ts` auto-merged). Ran `npm install` to pick up new `dotenv` dep from `feat/opensource` merge train (commit `f58b468`). Created local `.env` with dev endpoints (`GITCHAT_API_URL=https://api-dev.gitchat.sh/api/v1`, `GITCHAT_WS_URL=https://ws-dev.gitchat.sh`, `GITCHAT_WEBAPP_URL=https://dev.gitchat.sh`, `GITCHAT_GITHUB_CLIENT_ID=Ov23liXf7KFWwKzcOHE0`), values taken from `git show f58b468^:src/config/index.ts` — NOT fabricated. `npm run compile` clean (0 errors, 8 pre-existing warnings). `.env` is gitignored; `.env.example` already on develop. Discovered local `develop` branch had diverged with 1 stale commit `4f90ea0 Merge PR #103` (Vincent's phantom pin fix land) vs origin/develop 99 ahead — did NOT destructively reset local `develop`, bypassed by merging `origin/develop` directly into `hiru-uiux`. Pending user confirmation to push `hiru-uiux` (15 ahead / 0 behind `origin/hiru-uiux`).
- 2026-04-21: Planned two-phase work for today — #133 Part 1 finish (QA + PR `hiru-uiux` → `develop`) then resume #139 on branch `hiru-admin-role` (PR #157 DRAFT). BE #133 fix `gitchat-webapp#33` confirmed MERGED 2026-04-20 09:12 UTC — payload shape compatible with existing FE `type=new_message` routing, no FE code change needed. Deploy to `api-dev` unconfirmed — step 1 of QA is to ask @beenvincent / @norwayishere. Part 2 (out-of-app OS-level) remains a research spike, out of scope this session. Plan captured in detail in "Today's Plan (2026-04-21)" above for future Claude check-in.
- 2026-04-20: Shipped #133 Smart notification hybrid toast Part 1. 10 new/modified files: types/pure router (`toast-renderer.ts`), stack reducer (`toast-stack-state.ts`), `NativeRenderer` extracting existing `vscode.window.showInformationMessage` flow, `WebviewRenderer` (postMessage + pending-action promise map, `setReady`/`handleAction`/`reset` lifecycle), `toast-stack.css` (280px card, gs-* tokens only, 11px font floor, slide+fade 180/150ms, prefers-reduced-motion), `toast-stack.js` IIFE (DOM cards, 4s timer with hover pause/resume, click body → primary, × → dismiss, auto-dismiss no-action), `toast-coordinator` refactored to emit `ToastSpec` + route via pure `selectRenderer(ctx)` (visible && focused → webview else native, fork-editor `undefined` focus treated as true), `applyAction` maps 5 action kinds to existing commands (`gitchat.openChat`, `gitchat.openNotifications`) + `notificationStore.markRead`. Subagent-driven execution: 9 implementer dispatches + spec-compliance review + code-quality review per task, single batched feature commit per session rule. Unit tests: 13 cases (6 selectRenderer + 7 stack reducer) via Mocha TDD ui, pure-logic only (`npx mocha --ui tdd out/test/suite/<file>.test.js`), no VS Code harness. 2 mid-session fixes: (a) `toast-stack.js` singleton — plan prescribed `window._gsVsCode` pattern but `shared.js:3` declares `const vscode` at top-level which isn't reachable as a window property, so rewrote to `var vscodeApi = (typeof vscode !== 'undefined') ? vscode : acquireVsCodeApi()` matching `notifications-pane.js:7` precedent; (b) activation-order race — `exploreWebviewModule` + `notificationsModule` both in `parallelModules` (`Promise.allSettled`), so `exploreWebviewProvider?.setWebviewRenderer(webview)` in notifications/index.ts could silently no-op and leave webview `ready=false` forever → hybrid degrades to native-only invisibly. Fix: added `toastCoordinator.getWebviewRenderer()` getter, removed the no-op line, pulled renderer from coordinator inside `ExploreWebviewProvider.resolveWebviewView` where webview is guaranteed live. Also restored coordinator-level route log (`[ToastCoordinator] route kind=X id=Y n=Z → webview/native (fallback)?`) after reviewer flagged lost observability from the old `toast (digest) count=N` log. Also added `clearTimeout(_toastVisTimer)` in `ExploreWebviewProvider.dispose()` after reviewer flagged memory-leak window. Batched commit: all changes (incl. spec/plan docs + contributor doc + today's plan) in ONE `feat(notifications)` commit. Manual QA matrix walk-through in Extension Dev Host deferred to launch-and-smoke after commit.
- 2026-04-20: Opened issue #152 for Vincent — wave auto-opened DM shows empty chat body, the "👋 X waved at Y" system message (per BE PR #22) is not appearing. FE trace confirmed renderer handles `msg.type === 'system'` correctly (`sidebar-chat.js:580-584`); empty chat means BE didn't return that row. Three suspected root causes listed on issue: BE PR #22 not deployed to api-dev, `sendSystemMessage` fire-and-forget race vs FE's immediate `getMessages`, or BE emits a non-`system` type. Handed off to Vincent to verify.
- 2026-04-20: Wrote spec + plan for #133 Smart notification hybrid toast (Part 1 only; Part 2 out-of-app deferred to a research spike). Artifacts: `docs/superpowers/specs/2026-04-20-smart-notification-hybrid-delivery-design.md` + `docs/superpowers/plans/2026-04-20-smart-notification-hybrid-delivery.md`. Routing: `view.visible && window.state.focused` → new webview toast (280px card top-right stack, 4s auto-dismiss, click body to open, × to dismiss no-mark-read, hover pause, max 3 cards); otherwise current native sticky toast. Coordinator refactored to emit `ToastSpec` + inject `ToastRenderer` pair (`NativeRenderer` + `WebviewRenderer`). Stack reducer + `selectRenderer(ctx)` kept pure for Mocha unit tests; webview IIFE JS verified manually. Fork-editor guard: `windowFocused === undefined` treated as `true` (observed from Ryan's 2026-04-15 log on VS Code fork focus gate unreliability). Session scope: spec + plan only, no implementation code.
- 2026-04-17: Reset `hiru-uiux` branch from `develop` after session-start review — prior 77-commit backlog preserved on `hiru-uiux-archive` (both local + remote). Clean slate to work on assigned bugs without carrying UI/UX session work not yet PR'd.
- 2026-04-17: Closed #58 with Phase 1–2 decision "no role system, equal stargazer members" + opened Phase 3 ticket #123 for GitHub-maintainer-based auto-admin (maintainer = admin when joining that repo's community). Rationale: first-joiner-random is unfair; GitHub has source-of-truth for maintainers.
- 2026-04-17: Closed #100 + #101 — Vincent shipped PR #116 (wave button survives re-renders via `pendingWaveLogins` Set) + PR #122 (auto-open DM after wave, depends BE gitchat-webapp#22 which creates DM + system msg on `POST /waves`). Spec change "wave creates DM immediately" PM-approved and shipped.
- 2026-04-17: Posted investigation comment on #121 (toast duration) — native `showInformationMessage` has no duration API; dropping action buttons would auto-dismiss 15s but loses 1-click "Open Chat" (native toast has no body-click handler). Recommended hybrid: custom webview toast when sidebar visible + window focused, native fallback otherwise. Awaiting PO direction.
- 2026-04-17: Opened #125 for Vincent — after `50550fd` wave auto-navigate, FE hits `"must follow on GitHub"` error inside the auto-opened DM because `gitchat.messageUser` → `apiClient.createConversation` goes through the follow-gated endpoint. BE PR #22 creates the DM internally but wave response doesn't return `conversation_id`. Proposed BE fix: return `conversation_id` in wave DTO so FE can navigate directly.
- 2026-04-17: Fixed Profile Card "Followed by" semantic bug. `renderFollowedBy` in `media/webview/profile-screen.js:221` was reading from `data.mutual_friends` (= "people we both follow" — intersection of followings), producing the same list as the Mutual Friends section. Added new field `followed_by_friends` in `ProfileCardData` (`src/types/index.ts`), computed as `targetFollowers ∩ myFollowing` in `profile-card-enrich.ts` (1 extra `getUserFollowers` call in parallel, 5min cache), and renderFollowedBy now reads from it. Two sections now show semantically distinct data: "Followed by" = people I follow who also follow target; "Mutual Friends" = people both of us follow.

- 2026-04-15: Renamed `window.ProfileCard` (full sidebar takeover) → `window.ProfileScreen`, and `window.ProfileCardHover` (hover popover) → `window.ProfileCard`. File renames: `profile-card.js/css` → `profile-screen.js/css`; `profile-card-hover.js/css` → `profile-card.js/css`. Rationale: the full-screen thing behaves like the Chat screen (navStack), not a card. The real "card" is the 220px hover popover. Plan docs (`docs/superpowers/plans/2026-04-14-profile-split.md`) already called it "Profile Screen" since 2026-04-14 — naming now matches code. Kept the `.gs-pc-*` CSS prefix on both files (option 2) to avoid churning 100+ CSS rules. 4 external `.show()` callers in explore.js + sidebar-chat.js updated to `window.ProfileScreen.show()`. Internal escalation in profile-card.js hover also updated. Rewrote skeleton HTML to match the new multi-section card layout (header row + stats + mutuals + top repos). Profile Card rewrote skeleton HTML to match the new multi-section card layout (header row + stats + mutuals + top repos). New skeleton CSS variants: `gs-pc-skel-xs/sm/md/lg`, `gs-pc-skel-btn-icon`, `gs-pc-skel-btn-pill`, `gs-pc-skel-section`, `gs-pc-skel-repo`. New `.gs-pc-warning--success` CSS modifier for green-tinted positive rows (eligible + mutual state).
- 2026-04-15: Accordion fill-height pattern iterated — Discover + Friends tabs: default body min-height 120 → 96px (~2 friend rows, ~48px each). Secondary sections capped at `max-height: 240px` (~5 rows) with internal scroll: Discover TEAMS + ONLINE NOW, Friends NOT ON GITCHAT. Primary sections (Discover PEOPLE + COMMUNITIES, Friends OFFLINE) flex-grow to fill. Friends ONLINE is a special case: natural content height (no cap, no grow) so it shows exactly N rows without empty gap — OFFLINE fills remaining space.
- 2026-04-15: Friends empty states now have meaningful icons and consistent pattern with Discover empty states — ONLINE `codicon-circle-slash`, OFFLINE `codicon-person`, NOT ON GITCHAT `codicon-rocket`. Discover empty states already had icons.
- 2026-04-15: Reduced follow/profile cache TTLs so follow-state changes reflect within minutes: `src/api/github.ts CACHE_TTL_MS` 1h → 5 min, `PROFILE_CACHE_TTL_MS` 30 min → 5 min.
- 2026-04-15: Chat composer input (`.gs-sc-input`) `max-height: 68px → calc(5 * 1.4em + 8px)` (~99px = 5 lines). Auto-resize handler in sidebar-chat.js now writes `scrollHeight + 2` instead of `scrollHeight` to avoid a sub-pixel rounding bug with `box-sizing: border-box` that left a residual scrollbar when the exact line count fit.
- 2026-04-15: Profile Card full overlay gained warning-row statuses for all non-self states, all using the same `.gs-pc-warning` pattern at the bottom of the card. (a) stranger + both-not-following → "Follow X to unlock DM" (existing, yellow). (b) stranger + followed_by → "Follow back" button + "X is already following you — follow back to unlock DM". (c) eligible + not-mutual → "X hasn't followed you back yet" (info icon, yellow). (d) eligible + mutual → "You and X follow each other" (heart-filled icon, green success variant). (e) not-on-gitchat → "X is not on GitChat yet — invite them to join". New `.gs-pc-warning--success` CSS modifier (green tint + success icon color). `followed_by` comparison in enrichProfile switched to lowercase to handle GitHub's case-insensitive logins. Bumped `PROFILE_CACHE_KEY` → `profileCard.hostCache.v2` to evict stale cached entries that had the pre-fix wrong `follow_status` values.
- 2026-04-15: Friends tab empty-state bodies equalized — default body min-height 64px → 120px so ONLINE ("No friends online") and NOT ON GITCHAT ("Coming soon") appear at the same height. Reverted the `.gs-chat-list display: flex` change that was breaking Chat tab filter-bar layout; `#discover-content` and `#friends-content` now use `height: 100%` instead of `flex:1` to fill parent naturally.
- 2026-04-15: Fixed issue #68 — `.gs-sc-input` padding `8px 0 → 4px 0`, removed `min-height: 32px`. Chat footer input now aligns with media modal caption proportions (less bulky, textarea auto-grows from small). `font-size: --gs-font-base` was already fixed earlier this session.
- 2026-04-15: Discover + Friends tabs now use a fill-height accordion layout — primary sections flex-grow and scroll internally, secondary sections take natural height. Discover primary = PEOPLE + COMMUNITIES; secondary = TEAMS + ONLINE NOW. Friends primary = OFFLINE only; secondary = ONLINE + NOT ON GITCHAT. Implementation: (1) `buildAccordionSection` now emits per-section class marker `.gs-accordion-section--{tab}-{key}` so CSS can target individual sections; (2) `.gs-accordion-header flex-shrink: 0` in shared.css prevents header collapse when parent flex is tight; (3) `.gs-chat-list` became `display: flex; flex-direction: column; min-height: 0` so `#discover-content` and `#friends-content` can receive flex:1 from parent; (4) `#discover-content` and `#friends-content` inline styles changed `overflow-y:auto → overflow:hidden` (no outer scroll, only inner). All expanded bodies get `min-height: 64px` floor so empty-state content doesn't trigger inner scrollbar; primary sections get `min-height: 120px`. Also tightened `.gs-sc-input font-size: sm → base` per UX request. Pattern documented in `docs/design/UI-PATTERNS.md` under "Fill-height accordion (primary + secondary sections)".
- 2026-04-15: Renamed working branch `hiru-uiux-2 → hiru-uiux` locally + on remote. Old remote `origin/hiru-uiux-2` deleted after confirmation.
- 2026-04-15: `.gs-sc-status letter-spacing: -2px → -5px` — double checkmark "✓✓" now visually stacked tight instead of loose.
- 2026-04-15: Fixed chat bubble jitter when seen-avatar state toggles. Root cause: `.gs-sc-meta` had no min-height, so when seen-avatars (14px + 1.5px×2 border = 17px) appeared next to the 10px-font timestamp, the meta row jumped from ~13px to 17px, shifting every bubble above. Fix: `.gs-sc-meta { min-height: 17px }` reserves the height unconditionally. Also added wave error handler improvement: `discover:wave` 400 responses now surface the BE error message in the toast (`Failed to wave at @X (400): already_waved`) and treat known terminal codes (`already_waved|mutual|blocked|self`) as success like 403.
- 2026-04-15: Added slide-up entrance animation for newly appended chat bubbles. `.gs-sc-msg-row.gs-sc-msg-enter` = 220ms `translateY(8px) → 0` + opacity fade, ease-out, respects `prefers-reduced-motion`. JS adds the class in two places: `appendMessage` (realtime incoming + outgoing finalization) and the temp-outgoing insertion path. `animationend` handler uses `e.target !== newRow` check to ignore child animations. Only animates the newest row per append — initial full-render `renderMessages` path is not touched so chat history doesn't cascade-animate on open. First attempt caused whole-screen jitter because `.gs-sc-meta` had no reserved height for seen-avatars — fixed separately; animation re-applied after.
- 2026-04-15: Discover search-result row click now opens the in-sidebar Profile Card overlay instead of the legacy `ProfilePanel` editor-tab webview. `media/webview/explore.js` row handler for `.friend-row[data-search-result="1"]` calls `window.ProfileCard.show(login)` instead of posting `chatOpenProfile`. Removed the dead `case "chatOpenProfile"` and unused `ProfilePanel` import in `src/webviews/explore.ts`. `ProfilePanel` class itself retained since `src/commands/index.ts` still uses it for the `gitchat.viewProfile` command. Non-search person rows still open DM as before.
- 2026-04-15: Fixed issue #42 — notification row avatar alignment + size. `media/webview/notifications-pane.css`: `.notif-p-item align-items` flex-start → center (single-line text now centered with avatar), `.notif-p-avatar-wrap`/`.notif-p-avatar` 40×40 → 36×36 matching `--gs-avatar-md` per DESIGN.md token list. Type-badge overlay (18px, offset -3px) still visually balanced at 36px — no badge tweak needed.
- 2026-04-15: Fixed issue #45 — reordered Discover tab sections in `media/webview/explore.js buildDiscoverHtml`: TEAMS promoted to top (above PEOPLE and COMMUNITIES), new order = TEAMS → PEOPLE → COMMUNITIES → ONLINE NOW. Rationale per issue: contributed-repo teams are the highest-signal surface for active coders. Empty-state blocker mitigated by the fact that TEAMS is collapsed by default (`state.teams === true`), so an empty promoted section is only a single header bar — not visually dominant. No BE work required; companion issue #44 (Teams population) still open.
- 2026-04-15: Committed Prettier auto-format on `media/webview/sidebar-chat.css` as a separate `style(chat): format sidebar-chat.css` commit — no semantic change, just one-liner CSS rules expanded to multi-line by editor format-on-save.
- 2026-04-15: Hover profile card (`profile-card-hover.js`) now hides the Message button when the user is already viewing the DM with that person. Implementation: `sidebar-chat.js` writes `window.__gsActiveDmLogin = otherLogin` in `init` (only when `!isGroup && otherLogin`) and clears it on state reset. `renderPrimaryBtn` in hover popover returns empty string for `eligible` state when `data.login === window.__gsActiveDmLogin` — View Profile stays as the sole action. Scope narrow: only DM 1:1 (groups still show Message to allow starting a private DM with a member), and only the hover popover (full Profile Card overlay still shows Message for jump-to-DM use cases from other contexts).
- 2026-04-15: Replaced `.gs-main-tab` active top-accent implementation for the second time. Prior: `::before { position: absolute; top: 0; height: 1px }` pseudo. Symptom: when switching between main tabs (Chat/Friends/Discover/Noti), the 1px accent intermittently dropped on the currently-active tab — same sub-pixel compositor rounding bug that killed the earlier `box-shadow: inset` approach. Root cause: absolute-positioned pseudo creates its own compositor layer, which VS Code's webview renderer can still drop during pane slider transform flushes. Fix: bypass pseudo entirely — add `border-top: 1px solid transparent` to base `.gs-main-tab` (reserves space always, avoids 1px layout shift on activation) and set `border-top-color: var(--gs-button-bg)` on `.active`. Padding normalised `8px 4px 7px` → `7px 4px` to keep total height unchanged. Borders live inside the box model and paint reliably through any compositing — final fix, no more pseudo/box-shadow.
- 2026-04-15: Synced `hiru-uiux-2` with `origin/develop` at start of session — 3 commits (amando cold email doc, Vietnamese IME chat fix, v1.0.4 bump) fast-forwarded in, pushed to `origin/hiru-uiux-2`.
- 2026-04-15: Fixed two UX bugs on Explore main tabs. (1) `restoreState()` in `media/webview/explore.js` only toggled the `.active` class on tab buttons when restoring last-used tab; panes were not switched so restored Friends/Discover/Noti came back with Chat pane still visible. Fix: if restored tab differs from HTML default (`chat`) and navStack isn't a pinned conversation, programmatically click the target `.gs-main-tab[data-tab=…]` so the click handler runs the full pane swap + data fetch path. (2) `.gs-main-tab.active` top accent (1px) was implemented via `box-shadow: inset 0 1px 0 --gs-button-bg`, which intermittently disappeared after popping back from a chat conversation — GPU compositing from the slider's `transform` transition was rounding the sub-pixel inset shadow away. Fix: kept 1px (match VS Code native style) but moved the accent to an absolute `::before` pseudo (`top:0 left:0 right:0 height:1px`), painted as a block, immune to sub-pixel rounding. `.gs-main-tab` gained `position: relative`; inactive tabs get a transparent `::before`, active tabs get `background: var(--gs-button-bg)`.
- 2026-04-15: WP8 Wave code shipped in commit `feat(wave): sender+receiver paths+DM follow-gate retry`. Verified via `npm run compile` green. Manual smoke deferred to multi-account QA.
- 2026-04-15: WP8 Wave implementation complete per spec §WP8. Scope corrected after 3 misreads: Wave is a PING (notification only), not a message or DM. Only in Discover → Online Now, only for non-mutual online users. DM is gated per spec §5A ("I must follow target"). Artifacts: `docs/superpowers/specs/2026-04-15-wp8-wave-design.md`, `docs/superpowers/plans/2026-04-15-wp8-wave.md`, `docs/sync/be-requirements-wave.md`. Code: apiClient.wave + waveRespond, discover:wave host handler, notifications-pane tap-to-respond with createConversation fallback, buildDiscoverOnlineRow per-row mutual check (Wave button only for non-mutuals via chatMutualFriends lookup).
- 2026-04-15: Removed WP6 Profile Card Wave button (stranger state) — violated spec §WP8 that restricts Wave to Discover → Online Now. Deleted `WaveMockStore` + `createWaveMockStore` from profile-card-mocks.ts, `_waveStore` field + `case "profileCard:wave"` handler from explore.ts, `.gs-pc-wave-emoji` CSS, wave action branch from profile-card.js + profile-card-hover.js switch.
- 2026-04-15: Fixed semantic bug in `profile-card-enrich.ts` — `followed_by` was computed via `getUserFollowers(target)` which actually answers "does current user follow target" (= `following`), not "does target follow current user". Fix: added `getUserFollowing(login)` helper to `src/api/github.ts` hitting GH `/users/:login/following`, and `enrichProfile` now uses `targetFollowing.some(f => f.login === currentUser)` for `followed_by`. `mutual_friends` calculation also updated to intersect `myFollowing ∩ targetFollowing` (= people both sides follow), replacing the broken `targetFollowers.filter(myFollowing)` logic. 60min cache TTL preserved. Bug affected every Profile Card shown since WP6 ship.
- 2026-04-15: `gitchat.messageUser` command gained follow-gate retry workaround. BE `POST /messages/conversations` caches GitHub follow state and can return 400 "You must follow this user on GitHub to send them a DM" even when the user just followed the target. Workaround: detect the error via regex `/follow this user on GitHub/i` → call `apiClient.syncGitHubFollows()` → retry `createConversation` once. If still fails, surface the error. Proper fix is BE-side cache invalidation on follow/unfollow — flagged to Ryan.
- 2026-04-15: Discover → Online Now corrected. Section source is `chatFriends.filter(online)` = people I follow one-way (following list), NOT mutuals. Per-row mutual check via `isMutualFriend(login)` against `chatMutualFriends` (from `getMyFriends.mutual`): mutual → chevron + row click opens DM, non-mutual → Wave button. Section label stays "ONLINE NOW" since content is mixed. Full non-mutual Online Now population awaits BE `GET /discover/online-now` (documented in `docs/sync/be-requirements-wave.md §3`).
- 2026-04-15: Moved all `docs/qa/be-requirements-*.md` files → `docs/sync/` — 5 files via `git mv` (scroll, search, profile-card, all, be-remaining-scroll) + new `be-requirements-wave.md`. Updated 4 path references in WP6 historical docs (`2026-04-14-profile-card.md` plan + spec). Contributor doc historical decision log left unchanged (snapshot record).
- 2026-04-15: Synced `hiru-uiux` with `develop` at start of session — 81 commits merged via fast-forward, pushed to `origin/hiru-uiux`. Remote was 81 commits behind local after WP6 merges yesterday.


- 2026-04-14: Profile UX two-tier: hover popover (quick peek) + Profile Screen (full takeover). Hover popover: new `profile-card-hover.js/css` (`window.ProfileCardHover`), 220px wide, 150ms open / 200ms close delays, prefetches on mouseenter to hide latency (fetch fires at t=0 in parallel with delay timer, data usually warm by the time popover mounts). Content: 48px avatar, name/handle, 2-line bio, Following/Followers stats, row of `[primary pill] [View Profile outline]` both using `.gs-btn` primitives. `bindTrigger` on `window.ProfileCard` now delegates to hover (click retired — VS Code has no mobile). Skeleton has `min-height: 200px` + content swap via `swapContent()` (in-place innerHTML update) so popover doesn't jump when data lands. Dedup inflight fetches via `_inflight` Set. Closes on Escape / scroll / mouseleave.
- 2026-04-14: Profile Screen = repurposed existing full card overlay (NOT a new file per plan's Option A). `.gs-pc-backdrop` converted from centered-popup-with-blur to full sidebar takeover (`flex-direction: column`, `overflow-y: auto`, solid `--gs-bg` bg, no border-radius/shadow). `.gs-pc-card` lost `max-width: 320px`, padding bumped to `calc(var(--gs-inset-x) * 2)` = 16px horizontal (standard 8px felt cramped for full takeover). Back button (`codicon-arrow-left` matching chat pin-view/search conventions, NOT chevron-left) positioned `top:8px left:4px`. Header rebuilt Twitter-style: `.gs-pc-header-row` flex with 72px avatar on left + `.gs-pc-header-actions` (gh icon + state icon + primary pill) right-aligned. `renderActions` → `renderHeaderActions`; dropped bottom `.gs-pc-actions` block and `.gs-pc-github-link` (moved to header gh icon). Scoped overrides `.gs-pc-header-actions .gs-btn-icon` restore the outline border that `.gs-btn-icon` strips (Twitter circle buttons, 32px); `.gs-pc-header-actions .gs-btn-primary` pills with `border-radius: 999px`. Name/handle/bio now left-aligned. State→buttons mapping: self=[gh, Edit]; eligible=[gh, mail-icon, Following]; stranger=[gh, heart-icon, Follow/Following]; not-on-gitchat=[gh, Invite]. Top repo rows became clickable with `data-pc-repo-owner/name` → new `profileCard:openRepo` postMessage case in explore.ts calls `vscode.env.openExternal(github.com/{owner}/{name})`.
- 2026-04-14: CLAUDE.md gained a new design-system rule: "Reuse existing components, don't invent new ones" — lists shared.css primitives (.gs-btn*, .gs-input, .gs-avatar, .gs-row-item, .gs-dropdown, .gs-sub-tab, .gs-main-tab, .gs-empty, .gs-filter-bar). Motivated by me initially creating a custom `.gs-pch-btn` for the hover popover instead of composing `.gs-btn .gs-btn-primary`. Rule carves out: component-scoped one-offs like `.gs-pc-*` are OK but their internals must still use primitives (e.g. action buttons inside the card use `.gs-btn`).
- 2026-04-14: Drafted plan to split profile UX into Profile Card (popup, shipped) + Profile Screen (dedicated sidebar view with full info). Plan at `docs/superpowers/plans/2026-04-14-profile-split.md`. Architecture: Profile Screen is a 4th hidden mode inside the existing Explore webview (Option A), not a new provider. 10 tasks; "See full profile" link inside card drills down; legacy ProfilePanel (`src/webviews/profile.ts`) retires once screen reaches parity. NOTE (2026-04-14 later): plan scope was simplified — Profile Screen ended up being the SAME file as the full card overlay, just restyled to full takeover. No new `profile-screen.js/ts`; legacy `ProfilePanel` still lives (retirement deferred).
- 2026-04-14: Profile Card expanded to match spec fullness: (1) added `is_self` + `top_repos: RepoSummary[]` to ProfileCardData — `is_self` sourced from host (authManager.login match) for reliable self-state detection independent of client `_currentUser` wiring; `top_repos` carries up to 3 repos from UserProfile.top_repos. (2) enrichProfile sets is_self + picks top 3 repos (owner/name/stars/language/description). (3) profile-card.js — determineState now checks `data.is_self` first; renderHtml composes `mutualBlock` (all non-self states) + `topReposBlock` (all states) + warning + actions; new `renderTopRepos()` produces a bordered list with slug/desc/lang/stars; renderMutual now uses shared `.gs-pc-section-header`. (4) Added universal "View on GitHub" link below primary/secondary actions for self/eligible/stranger (not-on-gitchat already had it as secondary button). CSS: new `.gs-pc-section-header`, `.gs-pc-top-repos`, `.gs-pc-repo-*`, `.gs-pc-github-link` — all `--gs-*` tokens, min font 11px.
- 2026-04-14: Profile Card rendered skeleton correctly but all fields came back undefined (Repos/Followers/Following shown as literal "undefined"). Root cause: BE wraps profile response as `{ profile: {...}, repos: [...] }`, and `apiClient.getUserProfile` only unwraps the outer envelope (`data.data ?? data`). ProfilePanel already had the unwrap pattern (`raw.profile ?? raw`) — mirrored it in explore.ts profileCard:fetch handler. Added `UserProfile` to explore.ts type imports.
- 2026-04-14: profile-card.js IIFE was crashing at load because it called `acquireVsCodeApi()` a second time (shared.js already calls it once as a top-level const). Second call throws "already acquired" → IIFE dies before `window.ProfileCard` is assigned. Fix: remove the inner `const vscode = ...` line; the IIFE resolves `vscode` from the outer cross-script scope that shared.js established.
- 2026-04-14: User menu "View Profile" button in explore.js now routes to `window.ProfileCard.show(login)` instead of the legacy `viewProfile` postMessage (which opened ProfilePanel editor webview). Keeps profile UX unified — self-state card already surfaces Edit Profile + Sign Out. Falls back to old path when ProfileCard is unavailable.
- 2026-04-14: Added `.gs-main-tab` — segmented top-level tab component. Moved from explore.css → shared.css so any webview can reuse. Active state uses top-accent (`box-shadow: inset 0 1px 0 --gs-button-bg`) + background lift to `--gs-bg` to visually merge with content area. Divider between siblings via `+` combinator. Distinct from `.gs-sub-tab` (underline style for secondary grouping within a single surface). Documented in UI-PATTERNS.md.
- 2026-04-14: WP6 Profile Card implementation complete on hiru-uiux. 5 new files + 4 modifications. Real data for identity/stats/follow_status/mutual_friends/mutual_groups via apiClient + github.ts intersections, mocks isolated in profile-card-mocks.ts for on_gitchat + Wave. 11 trigger points wired across explore.js + sidebar-chat.js via window.ProfileCard.bindTrigger. BE requirements captured in docs/qa/be-requirements-profile-card.md with documented strip paths (5min + 15min). No automated tests — verification via npm run check-types + manual smoke matrix.
- 2026-04-14: WP6 Profile Card Task 11 complete — static audit + full build pass. Findings: (1) message types host→card (profileCardData/profileCardError/profileCardActionResult) all match listeners; (2) all 9 profileCard:* messages from card→host have switch cases in explore.ts EXCEPT profileCard:signOut — fixed by adding the missing case delegating to gitchat.signOut command; (3) ProfileCardData shape matches enrichProfile return + profile-card.js renderHtml reads; (4) pronouns optional field never set by enrichProfile (UserProfile lacks it) — silent undefined, gracefully handled by JS, flagged for future BE work; (5) no raw --vscode-* in profile-card.css; (6) script load order correct (shared → profile-card → sidebar-chat → explore → notifications); (7) bindTrigger selectors match data attrs in HTML; (8) _currentUser set via setChatData/setChatDataDev — both sent by explore.ts host. npm run compile exit 0 (3 pre-existing test warnings only).
- 2026-04-14: WP6 Profile Card Task 10 complete — bound ProfileCard triggers: (1) @mentions — added renderMessageText() wrapping /@([a-zA-Z0-9-]+)/g into .gs-sc-mention[data-login] spans; bindProfileCardTriggers() extended to bind them; CSS added to sidebar-chat.css. (2) Group info panel — bound .gs-sc-gi-member-info inside .gs-sc-gi-member[data-login] after area.appendChild(panel); Remove btn already had stopPropagation. (3) Pin banner author — threaded pin.sender_login into .gs-sc-pin-text.gs-sc-pin-author[data-login] attr; bound after innerHTML set. (4) Search results — added data-sender attr to .gs-sc-search-result rows; bound .gs-sc-search-result-sender as trigger. (5) Reactions "who reacted" dropdown — SKIPPED: feature does not exist; reactions only show emoji+count chip, no dropdown. check-types exit 0, lint exit 0 (3 pre-existing warnings).
- 2026-04-14: WP6 Profile Card Task 9 complete — bound ProfileCard triggers in sidebar-chat.js. No avatar element exists in message bubbles so .gs-sc-sender[data-login] is the sole trigger (group-chat incoming only). Added bindProfileCardTriggers(container) helper; called after renderMessages (innerHTML), appendMessage (insertAdjacentHTML beforeend), olderMessages (afterbegin), and newerMessages (beforeend). jumpToMessageResult + jumpToDateResult route through renderMessages and are covered implicitly. Temp outgoing message has no .gs-sc-sender so skipped. check-types exit 0, lint exit 0 (3 pre-existing warnings).
- 2026-04-14: WP6 Profile Card Task 8 complete — bound ProfileCard triggers in explore.js: (1) renderChatConversation adds data-other-login on direct conv rows; renderChatInbox binds .gs-avatar inside .conv-item[data-other-login] after click/contextmenu handlers. (2) renderChatFriends binds .gs-avatar inside .friend-item[data-login] after existing handlers — row click still opens DM. (3) devRenderChatInbox: same data-other-login attr + binding on .chat-conv-item. (4) devRenderChatFriends binds .gs-avatar inside .chat-friend-item[data-login] — msg-btn stopPropagation already present. No mention/autocomplete in explore.js — skipped for v1. check-types exit 0, lint exit 0.
- 2026-04-14: WP6 Profile Card Task 7 complete — wrote profile-card.css (201 lines): backdrop/card entrance animation, avatar/name/handle/bio/stats/mutual/top-repo/warning/actions/error/skeleton-shimmer states, narrow-sidebar @media fallback. All tokens --gs-* only, no hardcoded colors. check-types exit 0, lint exit 0 (3 pre-existing warnings in test suite).
- 2026-04-14: WP6 Profile Card Task 6 complete — replaced profile-card.js stub with full overlay IIFE: 4 states (self/eligible/stranger/not-on-gitchat), 60s client-side cache, skeleton/error/data HTML renderers, postMessage actions (message/follow/unfollow/wave/invite/github/signOut/editProfile), mutual friends/groups block, Escape+backdrop+X close, transition-end cleanup with 300ms fallback. Public API: window.ProfileCard.show/close/isOpen/bindTrigger. check-types exit 0, lint exit 0 (3 pre-existing warnings in test suite).
- 2026-04-14: WP6 Profile Card Task 5 complete — wired 7 profileCard:* postMessage cases into explore.ts onMessage switch. Added imports for enrichProfile, createWaveMockStore/WaveMockStore, getUserStarred, fireFollowChanged. Added _waveStore field initialized in existing setContext() (no constructor change needed — context already threaded). authManager.login null-guarded with ?? "" for getUserStarred + enrichProfile calls. gitchat.messageUser confirmed to exist. check-types exit 0, lint exit 0 (3 pre-existing warnings in test suite).
- 2026-04-14: WP6 Profile Card Task 4 complete — replaced profile-card-enrich.ts stub with real orchestrator: enrichProfile() takes UserProfile + currentUserLogin + EnrichContext, fast-paths self case, calls getUserFollowers+getUserStarred in parallel, computes follow_status/mutual_friends (up to 8)/mutual_groups (up to 6, star-intersection) via Set lookups, delegates on_gitchat to mockOnGitchat(). top_repos field confirmed present on UserProfile as RepoSummary[] (owner/name/stars all exist). check-types exit 0, lint exit 0.
- 2026-04-14: WP6 Profile Card Task 3 complete — replaced profile-card-mocks.ts stub with real implementation: mockOnGitchat uses FORCED_OFFLINE blacklist (empty by default, override for manual QA), createWaveMockStore persists sent-wave state in ctx.globalState keyed by "profileCard.wavesSent", mirrors eventual BE rate limit (1 wave per target lifetime). check-types exit 0, lint exit 0.
- 2026-04-14: WP6 Profile Card Task 2 complete — implemented src/api/github.ts with real ghFetch wrapper (Bearer token from authManager.token, optional when signed out), getUserFollowers + getUserStarred with 1h in-memory Map cache, isFresh<T> guard. Array<T> cast syntax converted to T[] per ESLint rule. check-types exit 0, lint exit 0.
- 2026-04-14: WP6 Profile Card Task 1 complete — scaffolded profile-card.js/css, src/api/github.ts, profile-card-mocks.ts, profile-card-enrich.ts, added ProfileCardData to src/types/index.ts, wired CSS+JS into explore.ts getHtml(). Array<T> lint rule triggered on ProfileCardData mutual fields — fixed to T[] syntax per project ESLint config. check-types exit 0, lint exit 0 (3 pre-existing warnings in test suite, not ours).
- 2026-04-14: WP6 Profile Card implementation plan written at `docs/superpowers/plans/2026-04-14-profile-card.md` — 12 bite-sized tasks covering scaffold, github wrapper, mocks, enrichment, host router, overlay component (profile-card.js IIFE), CSS, 11 trigger bindings across explore.js + sidebar-chat.js, full smoke test, and BE requirements doc. Every task ends in a commit. Verification is `npm run check-types` + manual smoke (no webview JS test harness per spec §12.9).
- 2026-04-14: WP6 Profile Card design spec written at `docs/superpowers/specs/2026-04-14-profile-card-design.md`. Key decisions: (1) universal sidebar overlay attached to `document.body`, single `window.ProfileCard.show(username)` API, scope v1 = sidebar only (editor ProfilePanel stays for full view); (2) 4 states — self / eligible / stranger / not-on-gitchat, eligible rule = `follow_status.following && followed_by` (true mutual); (3) production feature not demo — real data via GitHub API intersection for `follow_status.followed_by`, `mutual_friends`, `mutual_groups` (communities only, star-based); (4) mocks isolated in `profile-card-mocks.ts` for only 2 fields that BE must provide: `on_gitchat` + `POST /waves`, strip paths documented; (5) all 11 trigger points wired via `bindTrigger(el, username)` helper across explore.js + sidebar-chat.js; (6) close via X / Escape / backdrop click, fade+slide-up 150ms animation matching existing sidebar-chat overlays.
- 2026-04-14: `--gs-msg-incoming` token changed from `--vscode-editor-inactiveSelectionBackground` (gave unwanted blue tint from theme selection color) to `color-mix(in srgb, var(--gs-button-bg) 16%, var(--gs-bg))` — subtle brand-tinted neutral lift above bg, theme-aware, replaces ~50% opacity themed tint with controlled mix. Affects both sidebar-chat and chat editor panel since they share the token.
- 2026-04-14: `.tab-badge` inside main tab uses 9px font — exception to the 11px minimum rule, only permitted for numeric counters inside a 16px pill. Not reusable for text.
- 2026-04-13: Drafted 15 GitChat sidebar screens in Pencil (300px width, VS Code dark) — Welcome, Onboarding overlay, Chat Inbox, Friends, Discover, 4 chat types (DM/Group/Community/Team), Group Create, Profile Card (eligible + stranger states), Wave, Founder Agent DM, Notifications. Built reusable components: ConvRow (universal list item), ChatHeader, ChatInput, TabBar, ChatHeader-Inbox
- 2026-04-13: Profile Card has 2 conditional states — eligible (Message + Following) vs stranger (Wave + Follow) — Wave is the fallback CTA when DM not allowed (covers spec gap where strangers had no way to start a conversation outside Discover)
- 2026-04-13: Tightened .claude/settings.json PreToolUse hook — blocks commit if any docs/contributors/*.md other than current user's is staged (cross-edit guard)
- 2026-04-13: Added husky pre-commit hook (.husky/pre-commit) — same guard but enforced for ALL clients (manual git commit, non-Claude users), installed via `prepare` script
- 2026-04-13: Created docs/contributors/akemi0x.md — moved 5 GitChat rebrand decisions Akemi mistakenly wrote into nakamoto-hiru.md back to her own doc
- 2026-04-10: Added PreToolUse hook in .claude/settings.json — blocks commit unless contributor doc is staged, enforces auto-update workflow
- 2026-04-10: Enhanced CLAUDE.md session instructions — detailed dau phien steps, explicit on-commit doc update rules
- 2026-04-10: Team workflow in CLAUDE.md — status+decisions format for user docs, rules in CLAUDE.md (no shared skills), all git actions require user confirmation
- 2026-04-08: Merge strategy for develop (210 commits) into hiru-uiux (158 commits, 27 conflicts) — keep hiru UI/UX files, keep develop features, manual merge for core files
- 2026-04-08: New shared components in shared.css: .gs-row-item, .gs-rank, .gs-sub-header/.gs-sub-tab, .gs-filter-bar, .gs-dropdown — replaced per-view duplicates
- 2026-04-08: Trending redesign — accordion → sub-tabs (Repos | People) with time range chips + search. Reuses gs-sub-tab from Chat
- 2026-04-08: .gs-row-item as single base class for all list rows — replaced .conv-item, .friend-item, .tr-card, .tp-card, .channel-item
- 2026-04-08: Rank badges — gold/silver/bronze top 3 with color-mix transparency, rank 4+ no background
- 2026-04-07: Unified Explore tabs (Chat | Feed | Trending) replacing 6 separate TreeView providers into single webview
- 2026-04-07: Tabbed navigation over split Activity Bar — VS Code API can't programmatically place views in secondary sidebar
- 2026-04-05: All view CSS must use --gs-* tokens exclusively, direct --vscode-* usage prohibited — enables consistent theming + single source of truth
- 2026-04-05: 3-tab Explore layout chosen over reducing density — each tab has clear focused concern
